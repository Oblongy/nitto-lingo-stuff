// Promoted by run_lscr_pipeline.py --promote-siblings
// Source: Lscr-105/original-style.js
// Sibling slot: 13-socket-server-to-client.js
// Do not edit this file directly — edit original-style.js and re-run the pipeline.

// Lscr-105 original-style JS translation
// Source: Lscr-105/original-style.lingo
// Socket message dispatcher — handles all inbound server push messages.
// msg = incoming property-list object; ac = msg.ac (action code)

function procSocket(msg) {
  var ac = msg.ac;

  // -----------------------------------------------------------------------
  // Login / Session
  // -----------------------------------------------------------------------

  if (ac === "L") {
    timeout(sockConnTO).forget();
    var s = msg.s;
    if (s === true) {
      sprite(flashSP).loginCBS(s, msg.ni, msg.ns, msg.tid, msg.trp, msg.trbp);
      _global.objRace.sendInterval = msg.ni;
      _global.objRace.secondBuffer = msg.ns;
      heartBeatTO = timeout(heartBeatTO).new(600000, "sendHeartbeat");
      _global.objRace.lagThreshold = parseFloat(msg.lft);
    } else {
      _global.isConnected = 0;
      sprite(flashSP).loginCBS(s);
    }

  } else if (ac === "LOUT") {
    _global.isConnected = 0;
    sprite(flashSP).noActivityCB();

  // -----------------------------------------------------------------------
  // Broadcast / Money / Car Updates
  // -----------------------------------------------------------------------

  } else if (ac === "B") {
    sprite(flashSP).showBroadcastCB(msg.m);

  } else if (ac === "NMONEY") {
    sprite(flashSP).addMoneyCB(msg.c);

  } else if (ac === "UCL") {
    sprite(flashSP).updateCarListCB(msg.d);

  } else if (ac === "UB") {
    sprite(flashSP).updateBalanceCB(msg.d);

  } else if (ac === "UP") {
    sprite(flashSP).updatePointsCB(msg.d);

  // -----------------------------------------------------------------------
  // NIM (Buddy / Inquiry) Messages
  // -----------------------------------------------------------------------

  } else if (ac === "GN") {
    sprite(flashSP).getNimMessage(msg.i, msg.n, msg.r, msg.m);

  } else if (ac === "SNE") {
    sprite(flashSP).sendNimCB(msg.s, msg.r);

  } else if (ac === "UNU") {
    sprite(flashSP).updateNimUser(msg.i, msg.s, msg.ul);

  } else if (ac === "GNL") {
    sprite(flashSP).addNimUser(msg.d);

  } else if (ac === "AUR") {
    if (msg.s === true) {
      sprite(flashSP).addSingleNimUser(msg.i, msg.n, msg.us, msg.r, msg.ul);
    }

  } else if (ac === "IU") {
    sprite(flashSP).inquiryNimUserCB(msg.s);

  } else if (ac === "IA") {
    sprite(flashSP).inquiryNimAnswerCB(msg.i, msg.n, msg.s);

  } else if (ac === "RIU") {
    sprite(flashSP).receiveNimInquiryCB(msg.i, msg.n);

  } else if (ac === "AU") {
    sprite(flashSP).allowNimUserCB(msg.s);

  } else if (ac === "DU") {
    sprite(flashSP).deleteNimUserCB(msg.s, msg.i);

  } else if (ac === "RU") {
    sprite(flashSP).removeNimUserCB(msg.i);

  } else if (ac === "BU") {
    sprite(flashSP).blockNimUserCB(msg.s);

  } else if (ac === "UU") {
    sprite(flashSP).unblockNimUserCB(msg.s);

  } else if (ac === "DIU") {
    sprite(flashSP).deleteNimInquiredUserCB(msg.s);

  } else if (ac === "DREQ") {
    sprite(flashSP).deleteNimInquireeUserCB(msg.i);

  } else if (ac === "GIR") {
    sprite(flashSP).getNimIncomingRequestsCB(msg.d);

  } else if (ac === "GOR") {
    sprite(flashSP).getNimOutgoingRequestsCB(msg.d);

  // -----------------------------------------------------------------------
  // Race Chat Rooms
  // -----------------------------------------------------------------------

  } else if (ac === "LRCR") {
    sprite(flashSP).chatListRoomCB(msg.d);

  } else if (ac === "LRCR2") {
    sprite(flashSP).chatListRoom2CB(msg.d);

  } else if (ac === "CRC") {
    var s = msg.s;
    if (s === true) {
      sprite(flashSP).chatCreateRoomCB(s, msg.d);
    } else {
      sprite(flashSP).chatCreateRoomCB(s);
    }

  } else if (ac === "JR") {
    sprite(flashSP).chatJoinCB(msg.s);

  } else if (ac === "LRCU") {
    sprite(flashSP).chatListUsersCB(msg.d);

  } else if (ac === "UCU") {
    sprite(flashSP).chatUpdateUserCB(msg.d);

  } else if (ac === "GC") {
    sprite(flashSP).chatGetCB(msg.c, msg.u, msg.m);

  } else if (ac === "KRC") {
    sprite(flashSP).chatKickCB(msg.d);

  // -----------------------------------------------------------------------
  // Team
  // -----------------------------------------------------------------------

  } else if (ac === "TKCK") {
    sprite(flashSP).teamKickCB(msg.s);

  } else if (ac === "TCR") {
    sprite(flashSP).teamChangeRoleCB(msg.s);

  } else if (ac === "TUB") {
    sprite(flashSP).teamUpdateDealerMaxBetCB(msg.s);

  } else if (ac === "TDEP") {
    sprite(flashSP).teamDepositCB(msg.s);

  } else if (ac === "TWDL") {
    sprite(flashSP).teamWithdrawalCB(msg.s);

  } else if (ac === "TQ") {
    sprite(flashSP).teamQuitCB(msg.s);

  } else if (ac === "TACPT") {
    sprite(flashSP).teamAcceptCB(msg.s);

  } else if (ac === "TDSP") {
    sprite(flashSP).teamDisperseCB(msg.s);

  } else if (ac === "TNR") {
    sprite(flashSP).teamRoleCB(msg.d, msg.m);

  } else if (ac === "TCRT") {
    var s = msg.s;
    if (s === true) {
      sprite(flashSP).teamCreateCB(s, msg.id);
    } else {
      sprite(flashSP).teamCreateCB(s);
    }

  } else if (ac === "TLSD") {
    sprite(flashSP).teamStepDownCB(msg.s);

  // -----------------------------------------------------------------------
  // Race Staging Lights
  // -----------------------------------------------------------------------

  } else if (ac === "R1") {
    _global.objRace.startLightTimer("1", msg.td);

  } else if (ac === "R2") {
    _global.objRace.startLightTimer("2", msg.td);

  } else if (ac === "R4") {
    _global.objRace.startLightTimer("s", msg.td);

  } else if (ac === "R1T") {
    _global.objRace.startLightTimer("1T", msg.td);

  } else if (ac === "R2T") {
    _global.objRace.startLightTimer("2T", msg.td);

  } else if (ac === "R4T") {
    _global.objRace.startLightTimer("sT", msg.td);

  } else if (ac === "R3") {
    _global.objRace.startLightTimer("b", msg.td);
    _global.objRace.guid = msg.guid;

  } else if (ac === "UR") {
    _global.objRace.startLightTimer("p", msg.td);
    _global.objRace.guid = msg.guid;

  } else if (ac === "OR") {
    _global.objRace.startLightTimer("o", msg.td);

  } else if (ac === "RINP") {
    sprite(flashSP).raceEngineRaceInProgressCB(msg.d);

  // -----------------------------------------------------------------------
  // KOTH (King of the Hill)
  // -----------------------------------------------------------------------

  } else if (ac === "JK") {
    sprite(flashSP).chatKOTHJoinCB(msg.s);

  } else if (ac === "KU") {
    sprite(flashSP).chatKOTHUsersCB(msg.d);

  } else if (ac === "NKR") {
    sprite(flashSP).chatKOTHNewRacerCB(msg.aid, msg.acid);

  } else if (ac === "RLE") {
    sprite(flashSP).chatKOTHLeaveCB(msg.aid, msg.ik);

  } else if (ac === "NR") {
    sprite(flashSP).chatKOTHNRaceCB(msg.d);

  } else if (ac === "KRDY") {
    sprite(flashSP).raceKOTHReadyOpponentCB();

  } else if (ac === "KR") {
    var s = msg.s;
    if (s === true) {
      sprite(flashSP).raceKOTHReadyCB(s, msg.t, msg.t2);
    } else {
      sprite(flashSP).raceKOTHReadyCB(s);
    }

  } else if (ac === "KOK") {
    sprite(flashSP).raceKOTHOKCB(msg.d, msg.t);

  } else if (ac === "SV") {
    sprite(flashSP).chatCheerVoteCB(msg.s);

  } else if (ac === "V") {
    sprite(flashSP).chatCheerGetVoteCB(msg.a, msg.d, msg.i);

  } else if (ac === "RWIN") {
    sprite(flashSP).raceKOTHResultCB(msg.d);

  } else if (ac === "KD") {
    sprite(flashSP).raceKOTHFinishCB(msg.d);

  } else if (ac === "IO") {
    if (_global.objRace.raceType === "RIVAL" || _global.objRace.raceType === "TEAMRIVAL") {
      if (_global.objRace.raceType === "HT") {
        sprite(flashSP).raceKOTHIntOpponentCB(msg.d, msg.v, msg.a, msg.t);
      } else {
        sprite(flashSP).raceRIVIntOpponentCB(msg.d, msg.v, msg.a, msg.t);
      }
    } else {
      sprite(flashSP).raceKOTHIntOpponentCB(msg.d, msg.v, msg.a, msg.t);
    }

  } else if (ac === "I") {
    sprite(flashSP).raceSpectateCB(msg.d);

  } else if (ac === "KRTO") {
    sprite(flashSP).raceKOTHRTOpponentCB(msg.rt);

  } else if (ac === "KRT") {
    sprite(flashSP).raceKOTHRTCB(msg.r, msg.rt);

  } else if (ac === "LRC") {
    // no-op

  } else if (ac === "KTOUT") {
    sprite(flashSP).raceKOTHTimeoutCB(msg.s);

  // -----------------------------------------------------------------------
  // Quick Match (QM)
  // -----------------------------------------------------------------------

  } else if (ac === "JQH") {
    sprite(flashSP).chatQMHJoinCB(msg.s);

  } else if (ac === "JQB") {
    sprite(flashSP).chatQMBJoinCB(msg.s);

  } else if (ac === "LQH") {
    sprite(flashSP).chatQMHLeaveCB(msg.s);

  } else if (ac === "LQB") {
    sprite(flashSP).chatQMBLeaveCB(msg.s);

  } else if (ac === "QMRH") {
    sprite(flashSP).chatQMHRaceCB(msg.d);

  } else if (ac === "QMRB") {
    sprite(flashSP).chatQMBRaceCB(msg.d);

  } else if (ac === "RRY") {
    var s = msg.s;
    if (s === true) {
      sprite(flashSP).raceQMHReadyCB(s, msg.t, msg.t2);
    } else {
      sprite(flashSP).raceQMHReadyCB(s);
    }

  } else if (ac === "R") {
    sprite(flashSP).raceQMHIntOpponentCB(msg.d, msg.v, msg.a, msg.t);

  } else if (ac === "RRT") {
    sprite(flashSP).raceQMHRTOpponentCB(msg.rt);

  } else if (ac === "RDONE") {
    sprite(flashSP).raceQMHFinishCB(msg.d);

  } else if (ac === "QWIN") {
    sprite(flashSP).raceQMHResultCB(msg.d);

  } else if (ac === "TOUT") {
    sprite(flashSP).raceQMHTimeoutCB(msg.s);

  } else if (ac === "QMSM") {
    sprite(flashSP).chatQMLeaveCB(msg.s);

  } else if (ac === "QMRM") {
    sprite(flashSP).chatQMReceiveCB(msg.i, msg.n, msg.m);

  } else if (ac === "QMLR") {
    sprite(flashSP).chatQMLeaveCB(msg.s);

  } else if (ac === "QMUU") {
    sprite(flashSP).chatQMLeaveOpponentCB(msg.s, msg.i, msg.n);

  // -----------------------------------------------------------------------
  // Rival Race
  // -----------------------------------------------------------------------

  } else if (ac === "LR") {
    sprite(flashSP).chatRIVListCB(msg.d);

  } else if (ac === "RRQ") {
    var s = msg.s;
    if (s === true) {
      sprite(flashSP).chatRIVRequestCB(true, msg.r);
    } else {
      sprite(flashSP).chatRIVRequestCB(s);
    }

  } else if (ac === "RCLG") {
    sprite(flashSP).chatRIVChallengeCB(msg.d);

  } else if (ac === "RRS") {
    sprite(flashSP).chatRIVResponseCB(msg.s, msg.i);

  } else if (ac === "RDNY") {
    sprite(flashSP).chatRIVDeniedCB(msg.r);

  } else if (ac === "RN") {
    sprite(flashSP).chatRIVJoinCB(msg.d);

  } else if (ac === "RRA") {
    sprite(flashSP).chatRIVNRaceCB(msg.d);

  } else if (ac === "RIVTOUT") {
    sprite(flashSP).raceRIVTimeoutCB(msg.s);

  } else if (ac === "RL") {
    sprite(flashSP).chatRIVLeaveCB(msg.s);

  } else if (ac === "RLFT") {
    sprite(flashSP).chatRIVLeftCB(msg.d);

  } else if (ac === "RIVRDY") {
    sprite(flashSP).raceRIVReadyOpponentCB();

  } else if (ac === "RR") {
    var s = msg.s;
    if (s === true) {
      sprite(flashSP).raceRIVReadyCB(s, msg.t, msg.t2);
    } else {
      sprite(flashSP).raceRIVReadyCB(s);
    }

  } else if (ac === "RO") {
    sprite(flashSP).raceRIVOKCB(msg.t);

  } else if (ac === "RW") {
    sprite(flashSP).raceRIVResultCB(msg.d);

  } else if (ac === "RD") {
    sprite(flashSP).raceRIVFinishCB(msg.d);

  } else if (ac === "RIVRTO") {
    sprite(flashSP).raceRIVRTOpponentCB(msg.rt);

  } else if (ac === "RIVRT") {
    sprite(flashSP).raceRIVRTCB(msg.r, msg.rt, msg.i);

  // -----------------------------------------------------------------------
  // Human Tournament (HT)
  // -----------------------------------------------------------------------

  } else if (ac === "JT") {
    sprite(flashSP).htConnectCB(msg.s, msg.d);

  } else if (ac === "HQOK") {
    var s = msg.s;
    if (s === true) {
      sprite(flashSP).htQualifyOKCB(s, msg.d, msg.t);
    } else {
      sprite(flashSP).htQualifyOKCB(s, msg.d);
    }

  } else if (ac === "HQ32") {
    sprite(flashSP).htGetTop32CB(msg.d);

  } else if (ac === "HTQ32A") {
    sprite(flashSP).htTop32AddCB(msg.d);

  } else if (ac === "HTQ32R") {
    sprite(flashSP).htTop32RemoveCB(msg.i);

  } else if (ac === "HQR") {
    sprite(flashSP).htQualifyReadyCB(msg.s);

  } else if (ac === "HTQD") {
    sprite(flashSP).htQualifyingResultCB(msg.s, msg.d);

  } else if (ac === "HTQTO") {
    sprite(flashSP).htQualifyingTimeoutCB();

  } else if (ac === "HTEQ") {
    sprite(flashSP).htEndQualifyingCB(msg.d);

  } else if (ac === "HTA") {
    sprite(flashSP).htStartRaceCB(msg.d);

  } else if (ac === "HTAI") {
    sprite(flashSP).htStartRaceCB(msg.d);
    _global.objRace.init("HT");

  } else if (ac === "HTTO") {
    sprite(flashSP).htTimeoutCB(msg.s);

  } else if (ac === "HTTR") {
    sprite(flashSP).htTreeResultCB(msg.d);

  } else if (ac === "HTR") {
    var s = msg.s;
    if (s === true) {
      sprite(flashSP).htReadyCB(s, msg.t, msg.t2);
    } else {
      sprite(flashSP).htReadyCB(s);
    }

  } else if (ac === "HTRTO") {
    sprite(flashSP).htRTOpponentCB(msg.rt);

  } else if (ac === "HTRT") {
    sprite(flashSP).htRTCB(msg.r, msg.rt);

  } else if (ac === "HTD") {
    sprite(flashSP).htFinishCB(msg.d);

  } else if (ac === "HTW") {
    sprite(flashSP).htResultCB(msg.d);

  } else if (ac === "HTFT") {
    sprite(flashSP).htGetTournamentTreeCB(msg.d);

  } else if (ac === "HTS") {
    sprite(flashSP).htSpectateCB(msg.d);

  } else if (ac === "HTI") {
    sprite(flashSP).htInfoCB(msg.d);

  } else if (ac === "HTAP") {
    sprite(flashSP).htAwardPrizeCB(msg.d);

  // -----------------------------------------------------------------------
  // Team Rivals
  // -----------------------------------------------------------------------

  } else if (ac === "GTR") {
    sprite(flashSP).teamRivalsGetRacersCB(msg.d);

  } else if (ac === "TRGT") {
    sprite(flashSP).teamRivalsGetTeamsCB(msg.d);

  } else if (ac === "TRPRQ") {
    sprite(flashSP).teamRivalsPreRequestCB(msg.s, msg.d);

  } else if (ac === "TRRQ") {
    sprite(flashSP).teamRivalsRequestCB(msg.s, msg.d);

  } else if (ac === "TRCLG") {
    sprite(flashSP).teamRivalsIncomingChallengeCB(msg.d);

  } else if (ac === "TRCT") {
    sprite(flashSP).teamRivalsCounterCB(msg.s, msg.i, msg.m);

  } else if (ac === "TRRS") {
    sprite(flashSP).teamRivalsResponseCB(msg.s, msg.i, msg.m);

  } else if (ac === "TRDNY") {
    sprite(flashSP).teamRivalsDenyCB(msg.r);

  } else if (ac === "TRN") {
    sprite(flashSP).teamRivalsNewQueueCB(msg.d);

  } else if (ac === "TRO") {
    var s = msg.s;
    if (s === true) {
      _global.objRace.raceType = "TEAMRIVAL";
      executeCall("getonecarengine", "acid=" + msg.acid);
    } else {
      sprite(flashSP).teamRivalsWrongBracketCB();
    }

  } else if (ac === "TROB") {
    sprite(flashSP).teamRivalsOKCB(msg.d);

  } else if (ac === "TRTO") {
    sprite(flashSP).teamRivalsTimeoutCB();

  } else if (ac === "TRS") {
    sprite(flashSP).teamRivalsStartStagingCB(msg.t);

  } else if (ac === "TRR") {
    sprite(flashSP).teamRivalsStagedCB(msg.s, msg.i);

  } else if (ac === "TRRA") {
    sprite(flashSP).teamRivalsNewRaceCB(msg.d, msg.t);

  } else if (ac === "TRYRA") {
    sprite(flashSP).teamRivalsYourRaceCB(msg.d, msg.t);

  } else if (ac === "TRA") {
    sprite(flashSP).teamRivalsNRaceCB(msg.d);
    _global.objRace.goodCounterRT = null;
    _global.objRace.badCounterRT = null;

  } else if (ac === "TRINP") {
    sprite(flashSP).teamRivalsRaceInProgressCB(msg.d);

  } else if (ac === "TRF") {
    sprite(flashSP).teamRivalsFinishCB(msg.d);

  // -----------------------------------------------------------------------
  // Team Rivals Race (in-race callbacks)
  // -----------------------------------------------------------------------

  } else if (ac === "I2") {
    sprite(flashSP).raceTeamIntCB(msg.d, msg.v, msg.a, msg.t, msg.r);

  } else if (ac === "RIVRT2") {
    sprite(flashSP).raceTeamRTCB(msg.r, msg.rt);

  } else if (ac === "RD2") {
    sprite(flashSP).raceTeamFinishCB(msg.d);

  } else if (ac === "RW2") {
    sprite(flashSP).raceTeamResultCB(msg.d);

  // -----------------------------------------------------------------------
  // Election
  // -----------------------------------------------------------------------

  } else if (ac === "JE") {
    sprite(flashSP).JoinElectionCB(msg.s, msg.d);

  } else if (ac === "EIL") {
    sprite(flashSP).GetElectionInterviewListCB(msg.s, msg.d);

  } else if (ac === "ESI") {
    sprite(flashSP).ElectionStartInterviewCB(msg.d);

  } else if (ac === "ENI") {
    sprite(flashSP).ElectionNewIntervieweeCB(msg.i, msg.s);

  } else if (ac === "EO") {
    sprite(flashSP).ElectionOverCB();
  }
}
