// Promoted by run_lscr_pipeline.py --promote-siblings
// Source: Lscr-93/original-style.js
// Sibling slot: 01-frame1script.js
// Do not edit this file directly — edit original-style.js and re-run the pipeline.

// Lscr-93 original-style JS translation
// Source: Lscr-93/original-style.lingo
// MovieScript 9 - HTTP Response Dispatcher
// Handles all HTTP API responses from the net queue.
// response = parsed propList from net text result
// action   = _global.gNetID[i][0]  (the action string)

function exitFrame() {
  void _global.gNetID;
  void _global.checkedForOneClient;
  void _global.checkProcessResult;
}

function procResponse() {
  // Bootstrap: one-client process check
  if (_global.checkedForOneClient == null) {
    _global.checkedForOneClient = true;
    if (netError(_global.checkProcessResult) === "OK") {
      trace(netTextResult(_global.checkProcessResult));
      CheckNittoProcessesCB(netTextResult(_global.checkProcessResult));
    } else {
      CheckNittoProcessesCB("1");
    }
    _global.checkProcessResult = null;
    netDone(_global.checkProcessResult);
  }

  if (!(_global.i < _global.gNetID.length)) return;

  _system.gc();

  var entry = _global.gNetID[_global.i];
  if (netError(entry[1]) !== "OK") {
    var raw = netTextResult(entry[1]);
    if (_global.isEnc) raw = decrypt(raw);
    sprite(flashSP).showConnectionErrorCB("Protocol Error",
      "Connection protocol could not be established. Action:" + entry[0] + " (Error: 010)");
    if (raw === '"s",-100') {
      sprite(flashSP).showConnectionErrorCB("Session Timed Out",
        "I'm sorry, but your session has timed out, please restart the application. Last action: " + _global.lastAction);
    }
    return;
  }

  var raw = netTextResult(entry[1]);
  var response = parsePropList(raw);
  var action = entry[0];

  // -----------------------------------------------------------------------
  // Bootstrap / Cache / Login
  // -----------------------------------------------------------------------

  if (action === "getcache") {
    var xmlParser = new xtra("xmlparser");
    xmlParser.parseString(response.d);
    fetchCacheXMLDone(xmlParser.makeList().n2);
    xmlParser = null;

  } else if (action === "fbgetsession" || action === "login") {
    var s = response.s;
    if (s === true) {
      if (response.ftc === true) {
        openURL("http://www.facebook.com/connect/prompt_permissions.php?api_key=" + _global.facebookAPIKey +
          "&v=1.0&next=http://www.facebook.com/connect/login_success.html?xxRESULTTOKENxx&display=popup&ext_perm=publish_stream&enable_profile_selector=0");
      }
      _global.aid  = response.aid;
      _global.guid = response.guid;
      _global.cp   = encrypt(response.cp);
      _global.cw   = encrypt(response.cw);
      _global.cwc  = encrypt(response.cwc);
      connectSocket();
      sprite(flashSP).loginCB(response.s, response.d, response.at, response.am);
    } else if (s === -2 || s === -11 || s === -12) {
      if (s === -99) {
        sprite(flashSP).loginCB(response.s, response.d);
      } else if (s === -5) {
        sprite(flashSP).loginCB(response.s, response.d);
        _global.installerURL  = response.url;
        _global.installerFile = response.fn;
        _global.installerMD5  = response.md5;
      } else if (s === -14) {
        _global.facebookID = response.f;
        sprite(flashSP).fbGetSessionCB(response.s);
      } else if (s === -15) {
        sprite(flashSP).fbGetSessionCB(response.s);
      } else {
        sprite(flashSP).loginCB(response.s);
      }
    }

  } else if (action === "changehomemachine") {
    // no-op

  // -----------------------------------------------------------------------
  // Account / Profile
  // -----------------------------------------------------------------------

  } else if (action === "getinfo") {
    if (response.s === true) sprite(flashSP).getInfoCB(response.d);

  } else if (action === "getuser") {
    sprite(flashSP).getUserCB(response.d);

  } else if (action === "getusers") {
    sprite(flashSP).getUsersCB(response.d);

  } else if (action === "updatebg") {
    sprite(flashSP).updateBgColorCB(response.s);

  } else if (action === "updatedefaultcar") {
    sprite(flashSP).updateDefaultCarCB(response.s);

  } else if (action === "racersearch") {
    var s = response.s;
    if (s === true) sprite(flashSP).racerSearchCB(s, response.d);
    else            sprite(flashSP).racerSearchCB(s);

  } else if (action === "racersearchnopage") {
    var s = response.s;
    if (s === true) sprite(flashSP).racerSearchNoPageCB(s, response.d);
    else            sprite(flashSP).racerSearchNoPageCB(s);

  } else if (action === "changeemail") {
    sprite(flashSP).changeEmailCB(response.s);

  } else if (action === "changepassword") {
    sprite(flashSP).changePasswordCB(response.s);

  } else if (action === "forgotpw") {
    sprite(flashSP).forgotPasswordCB(response.s);

  } else if (action === "getstartershowroom") {
    sprite(flashSP).getStarterShowroomCB(response.d);

  } else if (action === "createaccount") {
    sprite(flashSP).createAccountCB(response.s);

  } else if (action === "verifyaccount") {
    sprite(flashSP).verifyAccountCB(response.s);

  } else if (action === "resendactivation") {
    sprite(flashSP).resendActivationCB(response.s);

  } else if (action === "agreetoterms") {
    sprite(flashSP).agreeToTermsCB(response.s);

  } else if (action === "activatepoints") {
    var s = response.s;
    if (s === true) sprite(flashSP).activatePurchaseCB(s, response.m, response.p, response.ma, response.va, response.d);
    else            sprite(flashSP).activatePurchaseCB(s);

  } else if (action === "activatemember") {
    var s = response.s;
    if (s === true) sprite(flashSP).activatePurchaseCB(s, response.m);
    else            sprite(flashSP).activatePurchaseCB(s);

  } else if (action === "getcode") {
    sprite(flashSP).getCodeCB(response.s, response.gid);

  } else if (action === "getbuddies") {
    sprite(flashSP).getUserBuddiesCB(response.d);

  } else if (action === "getavatarage") {
    compareAvatarAge(response.tids, "avatars");

  } else if (action === "getteamavatarage") {
    compareAvatarAge(response.tids, "teamAvatars");

  } else if (action === "uploadrequest") {
    sprite(flashSP).avatarUploadRequestCB(response.s);

  } else if (action === "getlocations") {
    sprite(flashSP).getLocationsCB(response.d);

  } else if (action === "movelocation") {
    sprite(flashSP).moveLocationCB(response.s, response.m);

  // -----------------------------------------------------------------------
  // Mail / Remarks
  // -----------------------------------------------------------------------

  } else if (action === "gettotalnewmail") {
    sprite(flashSP).getEmailTotalNewCB(true, response.im);

  } else if (action === "getemaillist") {
    sprite(flashSP).getEmailListCB(response.d);

  } else if (action === "getemail") {
    sprite(flashSP).getEmailCB(response.d);

  } else if (action === "deleteemail") {
    sprite(flashSP).deleteEmailCB(response.s, response.eid);

  } else if (action === "sendemail") {
    sprite(flashSP).sendEmailCB(response.d);

  } else if (action === "markemailread") {
    // no-op

  } else if (action === "addremark") {
    sprite(flashSP).addRemarkCB(response.s);

  } else if (action === "deleteremark") {
    var s = response.s;
    if (s === true) sprite(flashSP).deleteRemarkCB(true, response.i);
    else            sprite(flashSP).deleteRemarkCB(s);

  } else if (action === "getremarks") {
    sprite(flashSP).getRemarksCB(response.d);

  } else if (action === "getuserremarks") {
    sprite(flashSP).getUserRemarksCB(response.d);

  } else if (action === "setnondeletes") {
    sprite(flashSP).setRemarkNonDeletesCB(response.s);

  } else if (action === "setdeletes") {
    sprite(flashSP).setRemarkDeletesCB(response.s);

  // -----------------------------------------------------------------------
  // Car / Shop / Garage
  // -----------------------------------------------------------------------

  } else if (action === "getcarcategories") {
    sprite(flashSP).getCarCategoriesCB(response.d);

  } else if (action === "viewshowroom") {
    sprite(flashSP).viewShowroomCB(response.d);

  } else if (action === "getallcars") {
    var s = response.s;
    if (s === true) sprite(flashSP).getCarsCB(true, response.d);
    else            sprite(flashSP).getCarsCB(s);

  } else if (action === "getonecar") {
    var s = response.s;
    if (s === true) sprite(flashSP).getOneCarCB(true, response.d);
    else            sprite(flashSP).getOneCarCB(s);

  } else if (action === "getallotherusercars") {
    sprite(flashSP).getOtherUserCarsCB(response.s, response.d);

  } else if (action === "buycar") {
    var s = response.s;
    if (s === true) sprite(flashSP).buyCarCB(s, response.b, response.d);
    else            sprite(flashSP).buyCarCB(s);

  } else if (action === "sellcar") {
    var s = response.s;
    if (s === true) sprite(flashSP).sellCarCB(true, response.b);
    else            sprite(flashSP).sellCarCB(s);

  } else if (action === "getcarprice") {
    var s = response.s;
    if (s === true) sprite(flashSP).getCarPriceCB(true, response.p);
    else            sprite(flashSP).getCarPriceCB(s);

  } else if (action === "getallimcars") {
    var s = response.s;
    if (s === true) sprite(flashSP).getAllImCarsCB(true, response.d);
    else            sprite(flashSP).getAllImCarsCB(s);

  } else if (action === "getoutofimpound") {
    var s = response.s;
    if (s === true) sprite(flashSP).getCarOutOfImpoundCB(true, response.b);
    else            sprite(flashSP).getCarOutOfImpoundCB(s);

  } else if (action === "buydyno") {
    var s = response.s;
    if (s === true) {
      _global.objRace.startRace(response.d, response.t);
      _global.objRace.init("DYNO");
      var xmlParser = new xtra("xmlparser");
      xmlParser.parseString(response.d);
      var n2 = xmlParser.makeList().n2;
      xmlParser = null;
      sprite(flashSP).garageDynoBuyCB(s, response.b,
        n2.ATTRIBUTES.e, n2.ATTRIBUTES.u, n2.ATTRIBUTES.ad, n2.ATTRIBUTES.sl, n2.ATTRIBUTES.n);
    } else {
      sprite(flashSP).garageDynoBuyCB(s);
    }

  } else if (action === "changeboost") {
    sprite(flashSP).garageSetBoostCB(response.s);

  } else if (action === "changeairfuel") {
    sprite(flashSP).garageSetAFRatioCB(response.s);

  } else if (action === "getallcats") {
    sprite(flashSP).getPartCategoriesCB(response.d);

  } else if (action === "getwheelstirescats") {
    sprite(flashSP).getWheelsTiresPartCategoriesCB(response.d);

  } else if (action === "getallparts") {
    sprite(flashSP).getPartsCB(response.d, response.d1);

  } else if (action === "getallwheelstires") {
    sprite(flashSP).getWheelsTiresCB(response.d, response.d1);

  } else if (action === "buypart") {
    sprite(flashSP).buyPartCB("c", response.d1, response.d);

  } else if (action === "buyenginepart") {
    sprite(flashSP).buyPartCB("e", response.d1, response.d);

  } else if (action === "buyengine") {
    sprite(flashSP).buyPartCB("m", response.d1, response.d);

  } else if (action === "buypartugg") {
    sprite(flashSP).uggBuyCycleCB(response.d1, response.d);

  } else if (action === "installpart") {
    sprite(flashSP).installPartCB("c", response.d);

  } else if (action === "installenginepart") {
    sprite(flashSP).installPartCB("e", response.d);

  } else if (action === "swapengine") {
    sprite(flashSP).installPartCB("m", response.d);

  } else if (action === "uninstallpart") {
    sprite(flashSP).uninstallPartCB("c", response.d);

  } else if (action === "uninstallenginepart") {
    sprite(flashSP).uninstallPartCB("e", response.d);

  } else if (action === "sellcarpart") {
    var s = response.s;
    if (s === true) sprite(flashSP).sellPartCB("c", s, response.b);
    else            sprite(flashSP).sellPartCB("c", s);

  } else if (action === "sellenginepart") {
    var s = response.s;
    if (s === true) sprite(flashSP).sellPartCB("e", true, response.b);
    else            sprite(flashSP).sellPartCB("e", s);

  } else if (action === "sellengine") {
    var s = response.s;
    if (s === true) sprite(flashSP).sellPartCB("m", true, response.b);
    else            sprite(flashSP).sellPartCB("m", s);

  } else if (action === "getcarpartsbin") {
    sprite(flashSP).getCarPartsBinCB(response.d);

  } else if (action === "getpartsbin") {
    sprite(flashSP).getPartsBinCB(response.d);

  } else if (action === "getpartgroup") {
    sprite(flashSP).getPartGroupCB(response.d);

  } else if (action === "getcarperformanceincreases") {
    sprite(flashSP).getCarPerformanceIncreasesCB(response.d);

  } else if (action === "egue") {
    var s = response.s;
    if (s === true) sprite(flashSP).engineListCB(s, response.d);
    else            sprite(flashSP).engineListCB(s);

  } else if (action === "egep") {
    sprite(flashSP).engineGetAllPartsCB(response.d, response.d1);

  } else if (action === "ebep") {
    sprite(flashSP).engineBuyPartCB(response.d1);

  } else if (action === "esst") {
    var s = response.s;
    if (s === true) sprite(flashSP).engineSwapStartCB(true, response.d);
    else            sprite(flashSP).engineSwapStartCB(s);

  } else if (action === "esfi") {
    var s = response.s;
    if (s === true) sprite(flashSP).engineSwapFinishCB(true, response.d);
    else            sprite(flashSP).engineSwapFinishCB(s);

  // -----------------------------------------------------------------------
  // Paint / Plates / Vanity
  // -----------------------------------------------------------------------

  } else if (action === "getpaintcats") {
    sprite(flashSP).getPaintCategoriesCB(response.d);

  } else if (action === "getpaints") {
    sprite(flashSP).getPaintsCB(response.d);

  } else if (action === "buypaint") {
    var s = response.s;
    if (s > null) sprite(flashSP).buyPaintCB(s, response.b);
    else          sprite(flashSP).buyPaintCB(s);

  } else if (action === "getlicenseplates") {
    sprite(flashSP).getLicensePlatesCB(response.d);

  } else if (action === "buyplate") {
    var s = response.s;
    if (s > null) sprite(flashSP).buyPlateCB(s, response.b, response.i);
    else          sprite(flashSP).buyPlateCB(s);

  } else if (action === "buyvanity") {
    var s = response.s;
    if (s > null) sprite(flashSP).buyVanityCB(s, response.b);
    else          sprite(flashSP).buyVanityCB(s);

  // -----------------------------------------------------------------------
  // System Parts / Repair / Gears / Spare
  // -----------------------------------------------------------------------

  } else if (action === "getsystemparts") {
    var s = response.s;
    if (s > null) sprite(flashSP).getSystemPartsCB(s, response.d);
    else          sprite(flashSP).getSystemPartsCB(s);

  } else if (action === "systemswap") {
    sprite(flashSP).systemSwapCB(response.s, response.d);

  } else if (action === "getrepairparts") {
    var s = response.s;
    if (s === true) sprite(flashSP).getRepairPartsCB(true, response.d);
    else            sprite(flashSP).getRepairPartsCB(s);

  } else if (action === "repairparts") {
    sprite(flashSP).repairPartsCB(response.s);

  } else if (action === "getgearinfo") {
    var s = response.s;
    if (s > null) sprite(flashSP).getGearInfoCB(s, response.d);
    else          sprite(flashSP).getGearInfoCB(s);

  } else if (action === "buygears") {
    var s = response.s;
    if (s > null) sprite(flashSP).buyGearsCB(s, response.b);
    else          sprite(flashSP).buyGearsCB(s);

  } else if (action === "getspareprice") {
    sprite(flashSP).getSparePriceCB(response.s, response.p);

  } else if (action === "sellallspare") {
    sprite(flashSP).sellAllSpareCB(response.s, response.b);

  } else if (action === "getdescription") {
    if (response.s === true) sprite(flashSP).getPartDescriptionCB(response.d);

  // -----------------------------------------------------------------------
  // Team
  // -----------------------------------------------------------------------

  } else if (action === "teaminfo") {
    sprite(flashSP).teamInfoCB(response.d);

  } else if (action === "teamtrans") {
    var s = response.s;
    if (s === true) sprite(flashSP).teamTransCB(s, response.d);
    else            sprite(flashSP).teamTransCB(s);

  } else if (action === "teamsearch") {
    var s = response.s;
    if (s === true) sprite(flashSP).teamSearchCB(s, response.d);
    else            sprite(flashSP).teamSearchCB(s);

  } else if (action === "updateleadercomments") {
    sprite(flashSP).teamUpdateLeaderCommentsCB(response.s);

  } else if (action === "updateteamreq") {
    sprite(flashSP).teamUpdateTeamReqCB(response.s);

  } else if (action === "getteaminfo") {
    var s = response.s;
    if (s === true) sprite(flashSP).teamGetInfoCB(s, response.d);
    else            sprite(flashSP).teamGetInfoCB(s);

  } else if (action === "getallteamapps") {
    var s = response.s;
    if (s === true) sprite(flashSP).teamGetAllAppsCB(s, response.d);
    else            sprite(flashSP).teamGetAllAppsCB(s);

  } else if (action === "getallmyapps") {
    var s = response.s;
    if (s === true) sprite(flashSP).teamGetMyAppsCB(s, response.d);
    else            sprite(flashSP).teamGetMyAppsCB(s);

  } else if (action === "deleteapp") {
    sprite(flashSP).teamDeleteAppCB(response.s);

  } else if (action === "updateteamapp") {
    sprite(flashSP).teamUpdateAppCB(response.s);

  } else if (action === "addteamapp") {
    sprite(flashSP).teamAddAppCB(response.s);

  } else if (action === "setteamcolor") {
    sprite(flashSP).teamUpdateBgColorCB(response.s);

  // -----------------------------------------------------------------------
  // Race / Computer Tournament
  // -----------------------------------------------------------------------

  } else if (action === "getonecarengine") {
    var s = response.s;
    if (s === true) {
      trace("director getOneCarEngineCB");
      sprite(flashSP).getOneCarEngineCB(response.d);
      if (_global.objRace) {
        _global.objRace.startRace(response.d, response.t);
        _global.objRace.init(_global.objRace.raceType);
      }
    }

  } else if (action === "gettworacerscars") {
    var s = response.s;
    if (s === true) sprite(flashSP).raceGetTwoRacersCarsCB(s, response.d);
    else            sprite(flashSP).raceGetTwoRacersCarsCB(s, "");

  } else if (action === "getracerscars") {
    var s = response.s;
    if (s === true) sprite(flashSP).getRacersCarsCB(s, response.d);
    else            sprite(flashSP).getRacersCarsCB(s, "");

  } else if (action === "ctgr") {
    if (response.s === true) sprite(flashSP).ctGetRacersCB(response.d);

  } else if (action === "ctjt") {
    sprite(flashSP).ctJoinCB(response.s);

  } else if (action === "ctct") {
    sprite(flashSP).ctCreateCB(response.s);
    if (response.s === true) {
      sprite(flashSP).getOneCarEngineCB(response.d);
      _global.objRace.startRace(response.d, response.t);
    }
    _global.objRace.initComputerRace("CTQ", null);

  } else if (action === "ctrt") {
    var s = response.s;
    if (s === true) {
      sprite(flashSP).ctRequestCB(true, response.d, response.b);
      _global.objRace.initComputerRace("CT", response.b);
    } else {
      sprite(flashSP).ctRequestCB(s);
    }

  } else if (action === "ctst") {
    _global.guid = response.sk;
    var s = response.s;
    if (s === true) sprite(flashSP).ctSaveCB(s, response.d);
    else            sprite(flashSP).ctSaveCB(s);

  } else if (action === "practice") {
    if (response.s === true) _global.objRace.startRace(response.d, response.t);
    _global.objRace.init("P");
    sprite(flashSP).getOneCarEngineCB(response.d);
    sprite(flashSP).practiceCreateCB(response.s);

  } else if (action === "gethumantournaments") {
    var s = response.s;
    if (s === true) sprite(flashSP).htGetTournamentsCB(s, response.d);
    else            sprite(flashSP).htGetTournamentsCB(s);

  } else if (action === "joinhumantournament") {
    var s = response.s;
    if (s === true) {
      sprite(flashSP).getOneCarEngineCB(response.d);
      sprite(flashSP).htJoinTournamentCB(s, response.b, "");
      _global.objRace.raceType = "HTQ";
      _global.objRace.startRace(response.d, response.t);
    } else {
      sprite(flashSP).htJoinTournamentCB(s, "", response.d);
    }

  // -----------------------------------------------------------------------
  // Classified / Trade / Social
  // -----------------------------------------------------------------------

  } else if (action === "putcaronclassified") {
    sprite(flashSP).putCarOnClassifiedCB(response.s, response.d);

  } else if (action === "listclassified") {
    sprite(flashSP).listClassifiedCB(response.d);

  } else if (action === "getclassifieddetail") {
    sprite(flashSP).getClassifiedDetailCB(response.s, response.d);

  } else if (action === "buyfromclassified") {
    sprite(flashSP).buyFromClassifiedCB(response.s, response.d);

  } else if (action === "classifiedhistory") {
    sprite(flashSP).sellerClassifiedHistoryCB(response.d);

  } else if (action === "cancelclassified") {
    sprite(flashSP).cancelClassifiedCB(response.s, response.d);

  } else if (action === "requesttrade") {
    sprite(flashSP).requestTradeCB(response.s, response.d);

  } else if (action === "respondtrade") {
    sprite(flashSP).respondTradeCB(response.s, response.d);

  } else if (action === "canceltrade") {
    sprite(flashSP).cancelTradeCB(response.s, response.d);

  } else if (action === "getoutgoingtradehistory") {
    sprite(flashSP).outgoingTradeHistoryCB(response.d);

  } else if (action === "getpendingtrades") {
    sprite(flashSP).pendingTradesCB(response.d);

  } else if (action === "getwinsandlosses") {
    sprite(flashSP).getWinsAndLossesCB(response.d);

  } else if (action === "earnincentive") {
    sprite(flashSP).earnIncentiveCB(response.s, response.b);

  } else if (action === "logintwitter") {
    sprite(flashSP).loginTwitterCB(response.s, response.u);

  } else if (action === "setbadgevisible") {
    sprite(flashSP).setBadgeVisibleCB(response.s);

  } else if (action === "getblackcardprogress") {
    sprite(flashSP).getBlackCardProgressCB(response.d);

  // -----------------------------------------------------------------------
  // Election / Support / News
  // -----------------------------------------------------------------------

  } else if (action === "getelectionphase") {
    var s = response.s;
    if (s === true) sprite(flashSP).getElectionPhaseCB(true, response.p, response.t, response.i);
    else            sprite(flashSP).getElectionPhaseCB(s, null, null, response.i);

  } else if (action === "getelectionschedule") {
    sprite(flashSP).getElectionScheduleCB(response.d);

  } else if (action === "getnominatecount") {
    sprite(flashSP).getNominateCountCB(response.c);

  } else if (action === "nominate") {
    sprite(flashSP).nominateCB(response.s, response.e);

  } else if (action === "getelectionresult") {
    sprite(flashSP).getElectionResultCB(response.d);

  } else if (action === "electionvote") {
    sprite(flashSP).electionVoteCB(response.s, response.e);

  } else if (action === "activateaccount") {
    sprite(flashSP).activateAccountCB(response.s, response.e);

  } else if (action === "getsupport") {
    sprite(flashSP).getSupportCB(response.s, response.m, response.i, response.t);

  } else if (action === "getmisconductcount") {
    sprite(flashSP).getMisconductCountCB(response.oid, response.n, response.t);

  // -----------------------------------------------------------------------
  // Facebook
  // -----------------------------------------------------------------------

  } else if (action === "fbgettoken") {
    _global.facebookToken  = response.t;
    _global.facebookAPIKey = response.a;
    openURL("https://login.facebook.com/login.php?api_key=" + _global.facebookAPIKey +
      "&auth_token=" + _global.facebookToken + "&v=1.0&popup&req_perms=offline_access");
    sprite(flashSP).fbGetTokenCB(response.s);

  } else if (action === "fbremovefacebook") {
    sprite(flashSP).fbRemoveFacebookCB(response.s);

  } else if (action === "fbgetinviteurl") {
    var d = response.d;
    trace("here!");
    trace("info list!");
    trace(d);
    openURL("http://www.facebook.com/multi_friend_selector.php/?api_key=75142a34f8f8698fb895c3156093f9c8&content=This network is the best place on Facebook for viewing", "_blank");

  // -----------------------------------------------------------------------
  // Test Drive
  // -----------------------------------------------------------------------

  } else if (action === "checktestdrive") {
    var s = response.s;
    if (s === true) sprite(flashSP).checkTestDriveCB(s, response.d);
    else            sprite(flashSP).checkTestDriveCB(s);

  } else if (action === "accepttestdrive") {
    var s = response.s;
    if (s === true) sprite(flashSP).acceptTestDriveCB(s, response.h, response.m, response.p, response.d);
    else            sprite(flashSP).acceptTestDriveCB(s);

  } else if (action === "buytestdrivecar") {
    var s = response.s;
    if (s === true) sprite(flashSP).buyTestDriveCarCB(s, response.b);
    else            sprite(flashSP).buyTestDriveCarCB(s);

  } else if (action === "removetestdrivecar") {
    sprite(flashSP).removeTestDriveCarCB(response.s);

  } else if (action === "rejecttestdrive") {
    sprite(flashSP).rejectTestDriveCB(response.s);

  } else if (action === "changeshiftlightrpm") {
    sprite(flashSP).changeShiftLightRpmCB(response.s);

  // -----------------------------------------------------------------------
  // Special Events / News / Spotlight
  // -----------------------------------------------------------------------

  } else if (action === "joinspecialevent") {
    var s = response.s;
    if (s === true) sprite(flashSP).joinSpecialEventCB(s, response.b, response.d);
    else            sprite(flashSP).joinSpecialEventCB(s);

  } else if (action === "getnews") {
    var s = response.s;
    if (s === true) sprite(flashSP).getNewsCB(s, response.d);
    else            sprite(flashSP).getNewsCB(s);

  } else if (action === "getspotlightracers") {
    var s = response.s;
    if (s === true) sprite(flashSP).getSpotlightRacersCB(s, response.d);
    else            sprite(flashSP).getSpotlightRacersCB(s);

  } else if (action === "claimpendinguclprofit") {
    sprite(flashSP).claimPendingUCLProfitCB(response.s, response.b);

  // -----------------------------------------------------------------------
  // Late queue tail: installed engine parts / file downloads
  // -----------------------------------------------------------------------

  } else if (action === "getinstalledenginepartbyaccountcar") {
    var s = response.s;
    if (s === true) sprite(flashSP).getInstalledEnginePartsCB(s, response.d);
    else            sprite(flashSP).getInstalledEnginePartsCB(s);

  } else if (action === "avatars" || action === "teamavatars") {
    var entry = _global.fileNetID[_global.i];
    loadLocalAvatar(entry, entry[0], 1);

  } else if (action === "ugg") {
    loadLocalUgg(_global.fileNetID[_global.i], 1);

  } else if (action === "cache") {
    var entry = _global.fileNetID[_global.i];
    downloadCacheInProgress(getStreamStatus(entry[1]));

  } else if (action === "installer") {
    runInstaller(_global.fileNetID[_global.i]);
  }

  _global.gNetID.splice(_global.i, 1);
}
