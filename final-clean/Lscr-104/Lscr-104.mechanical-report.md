# Mechanical Lingo Reconstruction Report

Inventory pass:
- Deliverable `rightful.ls`: `598` lines, `76` visible `on` blocks.
- Handler inventory is byte-backed by the block table below: JSON metadata covers `LOGIN` through `CheckNittoProcessesCB`, and the lifted body also preserves embedded block `__embedded_lscr104_block75`.
- Obvious unresolved scan hits: `0` explicit `unk26` / `implicit_` markers, but several send/build handlers still preserve `param_id_*` argument slots where the matching block tables recover only the fixed command token and helper names.

Raw-evidence pass:
- Raw metadata: `C:\Users\Dilldo\Nitto\Lingo Stuff\clean\Lscr-104\root-artifacts\Lscr-104.json`
  - `scriptNumber = 4`
  - `handlersCount = 75`
  - `totalLength = 32904`
  - JSON handler inventory runs from `LOGIN` through `CheckNittoProcessesCB`
- Raw reconstructed surfaces:
  - `C:\Users\Dilldo\Nitto\Lingo Stuff\clean\Lscr-104\root-artifacts\Lscr-104.blocks.lingo`
  - `C:\Users\Dilldo\Nitto\Lingo Stuff\clean\Lscr-104\root-artifacts\Lscr-104.reconstructed.lingo`
  - per-block `pass-block*.md` tables under `root-artifacts`, with offsets from `0` through `27184`
- Late-helper provenance split:
  - the JSON handler inventory still reports `compiledLen = 5` with shallow `pushcons` / `pushcons` / `unk26` dispatch stubs for `IsMyProcessNameCorrect`, `CheckNittoProcesses`, and `CheckNittoProcessesCB`
  - the promoted late-helper bodies instead come from the deeper block payload slices and direct bin/header evidence at offsets `25976`, `26196`, `26544`, and `27184`
- Byte-pattern corroboration:
  - `C:\Users\Dilldo\Nitto\Lingo Stuff\clean\Lscr-104\root-artifacts\Lscr-104.pass77-whole-file-inventory.md`
  - offset `N` in the table below maps directly to `C:\Users\Dilldo\Nitto\Lingo Stuff\clean\Lscr-104\root-artifacts\Lscr-104.pass-blockN-table.md`

Reconstruction pass:
- `C:\Users\Dilldo\Nitto\Lingo Stuff\clean\Lscr-104\evidence\reconstructed\rightful.ls`: `615` lines.
- `C:\Users\Dilldo\Nitto\Lingo Stuff\clean\Lscr-104\evidence\reconstructed (1)\rightful.ls`: `596` lines.
- Deliverable `rightful.ls` started from `evidence\reconstructed (1)\rightful.ls`, but it now intentionally diverges at byte-backed lifts for blocks `4060`, `4576`, `5300`, `8392`, `10040`, `11288`, `11668`, `12148`, `14888`, `15520`, `18820`, `20076`, `20992`, `22244`, `26196`, `26544`, and `27184`.
- The longer `evidence\reconstructed\rightful.ls` was not promoted.

Readable-Lingo lift pass:
- Recovered Lingo artifact:
  - `C:\Users\Dilldo\Nitto\Lingo Stuff\final-clean\Lscr-104\rightful.ls`
- Byte-backed lifts promoted in this deliverable:
  - `LEAVEKOTH` at block `8392`: `pass-block8392-table.md` exposes raw instruction bytes `3b 00 04 35 00 05 3d 00 07 36 00 06 51`, which promote cleanly to `_global.objRace.raceType = KOTH`. The following `executeCall(getonecarengine, ("acid=" & param_ssm))` call stays a free call because the remaining bytes restart at `3b 00 08 39 ...` without a second `_global.objRace` receiver push.
  - The `54`/`55` same-slot serializer family is now promoted where the raw block proves both the write and the later reuse of that exact slot. `JOINRACECHAT` `4060` reuses slots `2` and `4` as `CRC` and `fromCharCode`; `LEAVERACECHAT` `4576` reuses slot `2` as `JRC`; `TEAMKICK` `5300` reuses slot `0` as `unescape`; `STG` `11288`, `RIVDONE` `14888`, `HTREADY` `20076`, and `TRGETRACERS` `22244` reuse slot `0` as `Math`; `int` `11668` and `KOK` `12148` reuse slots `0`, `1`, and `2` as `Math`, `floor`, and `ssm`; and `KRT` `10040`, `HTJOIN` `15520`, `HTQRT` `18820`, and `HTRT` `20992` reuse slots `0`, `1`, and `17` as the same rounded locals already stored at the top of those handlers (`Math`, `floor`, and `id_17` / `"after RD"`). The common byte-backed proof shape is an early store `54 00 NN ... 55 00 NN` followed by a later serializer read `54 00 NN` inside the same block.
  - `CheckNittoProcesses` at block `26196`: `pass-block26196-table.md`, `blocks.lingo`, and the raw block tail are now strong enough to promote the control shape itself and the compare constant. The raw sequence `42 57 00 00 51 ... 3c 00 05 13 07 00 08 43 57 00 00 51` proves `_player = 0`, a single conditional jump-over body, and the fallback write `_player = 1`, so the deliverable now carries a real `if ... end if` instead of a flat branch trace. The block tail also carries payload record `05 00 00 00 07 00 00 00 FF FF FF FF`, which resolves slot `5` to numeric `-1`. Within `Lscr-104`, that same opcode-`13` jump family only reappears in block `27184`, where the promoted `param_trace = "0"` gate is consistent with the same equality-via-negated-inequality family, so the deliverable now normalizes the branch to the readable equivalent `if _player.applicationName.search(_global.exeName) = -1 then` even though opcode `13` itself is still not independently decoded.
  - `CheckNittoProcessesCB` at block `26544`: `pass-block26544-table.md` exposes `3b 00 00 35 00 01 3f 12 07 00 03` for the explicit `_player.debugPlaybackEnabled = TRUE` compare-and-branch, `3b 00 02 3e 36 00 03 51` for `_global.checkedForOneClient = FALSE`, and `3b 00 02 3b 00 05 39 3b 00 02 35 00 06 3d 00 07 1b 3a 00 01 36 00 04 51` for `_global.checkProcessResult = getNetText((_global.mainURL & "oneclient.html?hey=1"))`. The same jump-over body is now carried as a real `if ... end if` in the deliverable. The `FALSE` constant is corroborated by block `27184`, where the raw `3e` constant also feeds the byte-backed `trace = false` reset in `blocks.lingo`.
  - `__embedded_lscr104_block75` at block `27184`: the raw leading assignment bytes `3e 57 00 00 51` match the same `FALSE` constant/write shape used in block `26544`, so the reset line now stays explicit as `trace = FALSE` instead of `trace = VOID`. The later branch region is now also lifted structurally from raw bytes instead of carried forward as a flat jump trace: `56 00 03 3f 16 07 00 0b 3f 57 00 00 51` proves `if CheckProcesses > TRUE then trace = TRUE`, `3b 00 09 39 3a 00 00 42 12 07 00 0a` proves the fallback `if IsMyProcessNameCorrect() = 0` compare, and `58 00 02 57 00 00 51` proves the fallback assignment is `trace = 2`, not the earlier reconstruction-backed `trace = "0"`.

JS mirror pass:
- Accepted local mirror:
  - `C:\Users\Dilldo\Nitto\Lingo Stuff\clean\Lscr-104\root-artifacts\Lscr-104.rebuilt-faithful.js`
- The mirror is not surfaced in `final-clean`.

| Block | Handler | Source | Offset | CodeLen | Lines | Unresolved | Names |
|---:|---|---|---:|---:|---:|---:|---:|
| 0 | LOGIN | named handler from metadata | 0 | 300 | 1 | 0 | 0 |
| 1 | SENDNIMMESSAGE | named handler from metadata | 384 | 32 | 1 | 0 | 6 |
| 2 | UPDATENIMUSER | named handler from metadata | 716 | 54 | 1 | 0 | 5 |
| 3 | DELETENIMUSER | named handler from metadata | 1044 | 29 | 1 | 0 | 4 |
| 4 | BLOCKNIMUSER | named handler from metadata | 1316 | 29 | 1 | 0 | 4 |
| 5 | UNBLOCKNIMUSER | named handler from metadata | 1580 | 29 | 1 | 0 | 4 |
| 6 | INQUIRYNIMUSER | named handler from metadata | 1848 | 29 | 1 | 0 | 4 |
| 7 | ALLOWNIMUSER | named handler from metadata | 2120 | 36 | 1 | 0 | 5 |
| 8 | NIMOKRESPONSE | named handler from metadata | 2432 | 47 | 1 | 0 | 4 |
| 9 | DELETENIMINQUIREDUSER | named handler from metadata | 2720 | 29 | 1 | 0 | 4 |
| 10 | GETNIMINREQUESTS | named handler from metadata | 3004 | 29 | 1 | 0 | 4 |
| 11 | GETNIMOUTREQUESTS | named handler from metadata | 3264 | 11 | 1 | 0 | 2 |
| 12 | LISTRACECHATROOMS2 | named handler from metadata | 3440 | 11 | 1 | 0 | 2 |
| 13 | CREATECHATROOM | named handler from metadata | 3648 | 47 | 1 | 0 | 4 |
| 14 | JOINRACECHAT | named handler from metadata | 4060 | 165 | 3 | 0 | 5 |
| 15 | LEAVERACECHAT | named handler from metadata | 4576 | 97 | 2 | 0 | 5 |
| 16 | LISTRACECHATUSERS | named handler from metadata | 4944 | 11 | 1 | 0 | 2 |
| 17 | SENDRACECHAT | named handler from metadata | 5120 | 11 | 1 | 0 | 2 |
| 18 | TEAMKICK | named handler from metadata | 5300 | 43 | 2 | 0 | 5 |
| 19 | TEAMCHANGEROLE | named handler from metadata | 5604 | 29 | 1 | 0 | 4 |
| 20 | TEAMUPDATEMAXBET | named handler from metadata | 5904 | 65 | 1 | 0 | 4 |
| 21 | TEAMDEPOSIT | named handler from metadata | 6240 | 47 | 1 | 0 | 4 |
| 22 | TEAMWITHDRAW | named handler from metadata | 6528 | 29 | 1 | 0 | 4 |
| 23 | TEAMQUIT | named handler from metadata | 6796 | 29 | 1 | 0 | 4 |
| 24 | TEAMACCEPT | named handler from metadata | 7036 | 11 | 1 | 0 | 2 |
| 25 | TEAMDISPERSE | named handler from metadata | 7208 | 29 | 1 | 0 | 4 |
| 26 | TEAMCREATE | named handler from metadata | 7492 | 47 | 1 | 0 | 4 |
| 27 | TEAMSTEPDOWN | named handler from metadata | 7776 | 36 | 1 | 0 | 5 |
| 28 | GETKOTH | named handler from metadata | 8056 | 11 | 1 | 0 | 2 |
| 29 | JOINKOTH | named handler from metadata | 8208 | 11 | 1 | 0 | 2 |
| 30 | LEAVEKOTH | named handler from metadata | 8392 | 78 | 3 | 0 | 11 |
| 31 | KCNT | named handler from metadata | 8904 | 11 | 1 | 0 | 2 |
| 32 | SENDVOTE | named handler from metadata | 9080 | 64 | 2 | 0 | 8 |
| 33 | KDONE | named handler from metadata | 9496 | 47 | 1 | 0 | 4 |
| 34 | KRT | named handler from metadata | 10040 | 537 | 4 | 0 | 15 |
| 35 | STG | named handler from metadata | 11288 | 72 | 2 | 0 | 6 |
| 36 | int | named handler from metadata | 11668 | 140 | 4 | 0 | 6 |
| 37 | KOK | named handler from metadata | 12148 | 158 | 4 | 0 | 6 |
| 38 | KREADY | named handler from metadata | 12604 | 46 | 2 | 0 | 8 |
| 39 | GETRIVALS | named handler from metadata | 12996 | 47 | 1 | 0 | 4 |
| 40 | RREQ | named handler from metadata | 13260 | 11 | 1 | 0 | 2 |
| 41 | RRSP | named handler from metadata | 13488 | 101 | 1 | 0 | 4 |
| 42 | RLVE | named handler from metadata | 13864 | 65 | 1 | 0 | 4 |
| 43 | RIVOK | named handler from metadata | 14144 | 11 | 1 | 0 | 2 |
| 44 | RIVREADY | named handler from metadata | 14292 | 28 | 2 | 0 | 6 |
| 45 | RIVRT | named handler from metadata | 14600 | 47 | 1 | 0 | 4 |
| 46 | RIVDONE | named handler from metadata | 14888 | 72 | 2 | 0 | 6 |
| 47 | HTJOIN | named handler from metadata | 15520 | 636 | 9 | 0 | 18 |
| 48 | HQOK | named handler from metadata | 16968 | 29 | 1 | 0 | 4 |
| 49 | HTGET32 | named handler from metadata | 17216 | 29 | 1 | 0 | 4 |
| 50 | HTGETTREE | named handler from metadata | 17460 | 11 | 1 | 0 | 2 |
| 51 | HTSPECTATE | named handler from metadata | 17620 | 11 | 1 | 0 | 2 |
| 52 | HTQUIT | named handler from metadata | 17780 | 11 | 1 | 0 | 2 |
| 53 | HTINFO | named handler from metadata | 17932 | 11 | 1 | 0 | 2 |
| 54 | HTQREADY | named handler from metadata | 18088 | 11 | 1 | 0 | 2 |
| 55 | HTQD | named handler from metadata | 18276 | 47 | 1 | 0 | 4 |
| 56 | HTQRT | named handler from metadata | 18820 | 537 | 4 | 0 | 15 |
| 57 | HTREADY | named handler from metadata | 20076 | 72 | 2 | 0 | 6 |
| 58 | HTD | named handler from metadata | 20448 | 47 | 1 | 0 | 4 |
| 59 | HTRT | named handler from metadata | 20992 | 537 | 4 | 0 | 15 |
| 60 | TRGETRACERS | named handler from metadata | 22244 | 72 | 2 | 0 | 6 |
| 61 | TRGETTEAMS | named handler from metadata | 22592 | 11 | 1 | 0 | 2 |
| 62 | TRPREREQUEST | named handler from metadata | 22752 | 11 | 1 | 0 | 2 |
| 63 | TRREQUEST | named handler from metadata | 22932 | 29 | 1 | 0 | 4 |
| 64 | TRCOUNTER | named handler from metadata | 23416 | 155 | 1 | 0 | 4 |
| 65 | TRRESPONSE | named handler from metadata | 23864 | 47 | 1 | 0 | 4 |
| 66 | TROK | named handler from metadata | 24168 | 47 | 1 | 0 | 4 |
| 67 | TRIVREADY | named handler from metadata | 24448 | 29 | 1 | 0 | 4 |
| 68 | JOINELECTION | named handler from metadata | 24728 | 47 | 1 | 0 | 4 |
| 69 | ELECTIONINTERVIEWLIST | named handler from metadata | 25000 | 11 | 1 | 0 | 2 |
| 70 | m | named handler from metadata | 25180 | 11 | 1 | 0 | 2 |
| 71 | CLOSEGAME | named handler from metadata | 25500 | 209 | 1 | 0 | 4 |
| 72 | IsMyProcessNameCorrect | named handler from metadata | 25976 | 11 | 1 | 0 | 2 |
| 73 | CheckNittoProcesses | named handler from metadata | 26196 | 40 | 5 | 0 | 5 |
| 74 | CheckNittoProcessesCB | named handler from metadata | 26544 | 43 | 4 | 0 | 8 |
| 75 | __embedded_lscr104_block75 | embedded block without handler metadata | 27184 | 145 | 16 | 0 | 14 |

Unresolved:
- The current local block table reports `0` explicit unresolved counts, and the promoted readable body did not surface additional `unk26` or `implicit_` markers during the inventory scan.
- The remaining `param_id_*` slots in `JOINRACECHAT`, `RRSP`, `TRCOUNTER`, `CLOSEGAME`, `KRT`, `HTQRT`, and `HTRT` are preserved intentionally. Matching byte-backed tables `pass-block4060-table.md`, `pass-block10040-table.md`, `pass-block13488-table.md`, `pass-block18820-table.md`, `pass-block23416-table.md`, and `pass-block25500-table.md` recover the fixed command token plus helper names, but they do not recover names for those extra parameter positions.
- The flat serializer family around `RRSP` `13488`, `TRCOUNTER` `23416`, and `CLOSEGAME` `25500` is now pinned more tightly. All three blocks open with the same ladder head `3b 00 00 39 3d 00 01 3b 00 02 35 00 03 39 58 00 1e 3a 00 01 1b 54 00 00`: a fixed command token (`RRQ`, `TRRQ`, or `M`), a repeated `String.fromCharCode(30)` separator build, and then monotonically increasing raw `54` slot reads. The ceiling differs only by block width: `RRSP` climbs through slots `0..4`, `TRCOUNTER` through `0..7`, and `CLOSEGAME` through `0..10`, each terminating with the same trailing `3a 00 01 51`. That makes these three handlers byte-backed flat serializer ladders, not partially lifted local-variable builders, so no extra field name beyond the proven names table is promoted into the executable body.
- The `54`/`55` serializer family is no longer a blanket blocker. Where the same block proves an early store `54 00 NN ... 55 00 NN` and a later serializer read `54 00 NN`, the deliverable now reuses the stored readable name for that exact slot. That is why `JOINRACECHAT` `4060` now reuses `CRC` and `fromCharCode`, `LEAVERACECHAT` `4576` now reuses `JRC`, `TEAMKICK` `5300` now reuses `unescape`, `STG` `11288`, `RIVDONE` `14888`, `HTREADY` `20076`, and `TRGETRACERS` `22244` now reuse slot `0` as `Math`, `int` `11668` and `KOK` `12148` now reuse slots `0..2` as `Math`, `floor`, and `ssm`, and `KRT` `10040`, `HTJOIN` `15520`, `HTQRT` `18820`, and `HTRT` `20992` now reuse slots `0`, `1`, and `17` as the same rounded locals already written at the top of those handlers.
- What remains blocked in that family is narrower: any later `54` read with no earlier same-slot `55` write, or with no safe recovered name, stays explicit as a raw `param_*` slot read. That is why `JOINRACECHAT` `4060` still leaves slots `0`, `1`, `3`, `5`, and `6` raw; `LEAVERACECHAT` `4576` still leaves slots `0`, `1`, and `3` raw; `STG` `11288`, `RIVDONE` `14888`, `HTREADY` `20076`, and `TRGETRACERS` `22244` still leave slot `1` raw; and the flat ladders `RRSP` `13488`, `TRCOUNTER` `23416`, and `CLOSEGAME` `25500` still remain pure raw slot chains because their later reads do not have the same-slot local-store proof.
- The `blocks.lingo` `op_55` census now makes that boundary exact. Within the readable serializer family, `op_55` only appears at `JOINRACECHAT` `4060` offsets `0x000A` and `0x0018`, `LEAVERACECHAT` `4576` offset `0x000A`, `TEAMKICK` `5300` offset `0x000A`, `KRT` `10040` offsets `0x0015`, `0x002E`, and `0x0047`, `STG` `11288` offset `0x0015`, `int` `11668` offsets `0x0015`, `0x002E`, and `0x0047`, `KOK` `12148` offsets `0x0015`, `0x002E`, and `0x0047`, `RIVDONE` `14888` offset `0x0015`, `HTJOIN` `15520` offsets `0x0015`, `0x002E`, and `0x0047`, `HTQRT` `18820` offsets `0x0015`, `0x002E`, and `0x0047`, `HTREADY` `20076` offset `0x0015`, `HTRT` `20992` offsets `0x0015`, `0x002E`, and `0x0047`, and `TRGETRACERS` `22244` offset `0x0015`. No additional serializer block in `Lscr-104.blocks.lingo` carries an `op_55` store, so there is no untouched same-slot write/read candidate left elsewhere in `Lscr-104`.
- That same census is also why the join/leave re-check is closed. `JOINRACECHAT` `4060` has literal same-slot proof only for slots `2` and `4`, so `CRC` and `fromCharCode` are the only safe reuse promotions there; `LEAVERACECHAT` `4576` has literal same-slot proof only for slot `2`, so `JRC` is the only safe reuse promotion there. The remaining serializer reads in those two handlers stay raw because no earlier `op_55` store targets those same slots.
- A residue sweep against the promoted same-slot handlers did not uncover any new removable standalone lines. In `JOINRACECHAT` `4060`, `LEAVERACECHAT` `4576`, and `TEAMKICK` `5300`, the earlier raw `unescape()(param_*)` reads are already fully consumed by the promoted assignment lines at `0x001E`, `0x002C`, and `0x001E`, and the later same-slot serializer reads are already folded into the final `ssm(...)` statements at `0x00B8`, `0x0074`, and `0x003E`. After those lifts, the only surviving standalone executable evidence surface in `final-clean\Lscr-104\rightful.ls` is the terminal `_player -- 0x003B` line in `CheckNittoProcesses` `26196`, and that line stays because the local tail bytes still stop at `56 00 00 05` without independently proving that the read can be dropped or promoted to an explicit `return`.
- The only remaining equivalent direct slot-read family in local `Lscr-104` evidence is internal to embedded block `27184`, not to any unresolved serializer ladder. The raw span beginning at the lifted `CheckProcesses(_global.exeName)` line carries consecutive write/read pairs `57 00 01 51 56 00 01`, `57 00 02 51 56 00 02`, and `57 00 03 51 56 00 03`, aligned to the already-promoted readable sequence `"result: " = CheckProcesses(_global.exeName)` at `0x003F`, `"0" = "result: ".split(",")` at `0x0050`, and `CheckProcesses = "0".length` at `0x005A`. Those direct slot reads are therefore already fully consumed by the lifted late-helper assignments and comparison at `0x0062`; they do not expose a new outbound serializer reuse and they do not create another removable standalone tail line.
- A whole-`root-artifacts` literal scan now closes the remaining direct-slot search more tightly. Outside the structured JSON `hexPreview` mirror for block `26196`, the only instruction-region `57 00` spans left in local `Lscr-104` evidence are `43 57 00 00 51 56 00 00 05` at the tail of `CheckNittoProcesses` `26196` and the embedded write/read sequence in `27184` beginning `3e 57 00 00 51 ... 57 00 01 51 56 00 01 ... 57 00 02 51 56 00 02 ... 57 00 03 51 56 00 03`. No `57 00` span appears in `pass-block4060-table.md` or `pass-block4576-table.md`, so there is no equivalent direct-slot proof left to reopen `JOINRACECHAT` or `LEAVERACECHAT`.
- The `56 00` side of that family is now equally closed. A root-artifact literal scan finds instruction-region `56 00` bytes only in the same two places: the `56 00 00` tail read before opcode `05` in `CheckNittoProcesses` `26196`, and the already-consumed embedded slot reads `56 00 01`, `56 00 02`, and `56 00 03` inside `27184`. No instruction-region `56 00` span appears in `pass-block4060-table.md`, `pass-block4576-table.md`, or `pass-block5300-table.md`, so there is no hidden direct-slot consumer left in `JOINRACECHAT`, `LEAVERACECHAT`, or `TEAMKICK` beyond the same-slot lifts already promoted.
- Late utility helpers are narrower now than the earlier staging report implied. `pass-block25976-table.md` exposes `3b 00 00 35 00 01 39 3a 00 00 51`, which is enough to keep `_player.quit()` as the promoted readable body for `IsMyProcessNameCorrect`; that handler no longer has a receiver-level blocker. `CheckNittoProcesses` block `26196` is partially promoted too: `pass-block26196-table.md` proves the `_player.applicationName.search(_global.exeName)` receiver/method chain, the initial `_player = 0` write, the single conditional jump-over body, and the fallback `_player = 1` write, so the control is now carried as a real `if ... end if`. The block tail also resolves slot `5` directly via payload record `05 00 00 00 07 00 00 00 FF FF FF FF`, so the `-1` compare constant is now byte-backed. The remaining blocker there is narrower: opcode `13` is still not independently decoded, but inside `Lscr-104` it only appears in blocks `26196` and `27184`, and the promoted `param_trace = "0"` gate in `27184` is consistent with the same equality-via-negated-inequality family, so the deliverable now uses the readable equivalent `= -1` branch instead of the older `if not (... <> -1)` spelling. `CheckNittoProcessesCB` is no longer in that unresolved bucket for its `_global.checkedForOneClient` and `_global.checkProcessResult` writes, and its explicit `= TRUE` predicate compare plus jump-over body are also byte-backed by `pass-block26544-table.md`.
- `CheckNittoProcesses` `26196` also has one last tail-specific blocker now pinned more tightly than before. The handler ends with raw bytes `... 43 57 00 00 51 56 00 00 05`: the `43 57 00 00 51` fallback write is already promoted as `_player = 1`, and `56 00 00` is strong enough to keep the trailing readable `_player` stack read at `0x003B`. But opcode `05` is not independently decoded anywhere else in the local late-helper block set, so the deliverable still does not promote that tail to an explicit `return _player`. The final line therefore remains the narrower `_player -- 0x003B` evidence surface in the readable artifact rather than a guessed return statement.
- That tail blocker is now exact at the block-mirror layer too: `Lscr-104.blocks.lingo` contains only one explicit `op_05` marker anywhere in the local readable mirrors, at `CheckNittoProcesses` `26196` offset `0x0027`, immediately after the surviving `_player` read at `0x003B`. With no second local `op_05` site to compare against, the current deliverable keeps `_player -- 0x003B` explicit instead of guessing whether opcode `05` is a real return terminator or some other finalizer.
- The `26196` suffix is also structurally closed now. The full 40-byte code window is `42 57 00 00 51 3b 00 00 35 00 01 35 00 02 39 3b 00 03 35 00 04 3a 00 01 3c 00 05 13 07 00 08 43 57 00 00 51 56 00 00 05`, so after the compare/jump pair there is only one fallback store `43 57 00 00 51` and one terminal slot read `56 00 00` before opcode `05`. There is no second trailing push, no extra dead store, and no empty branch stub after `0x003B`, which is why this remains a single unresolved return-like suffix instead of a removable fake standalone line.
- That means the unresolved `26196` surface is now only two opcode regions wide: the compare/jump family at `0x0018..0x001E` (`3c 00 05 13 07 00 08`), and the undecoded return-like suffix at `0x0024..0x0027` (`56 00 00 05`). Everything else in the handler is already consumed by promoted readable lines.
- The on-disk tail boundary for `26196` is closed too. With block file offset `30804`, prefix `4`, and marker `16`, the 40-byte code window starts at file offset `30824` and ends at `30863`. The unresolved compare/jump bytes occupy `30848..30854`, and the unresolved suffix `56 00 00 05` occupies the final four code bytes at `30860..30863`. There are no code bytes after opcode `05`, so the readable `_player -- 0x003B` line is not hiding a second post-tail residue line.
- The block-start side of that `26196` handoff is narrower now too. Its four-byte pre-marker `6c 75 65 00` at file offsets `30804..30807` is not a fresh late-helper payload fragment; it is byte-identical to the missing tail of `returnValue` from the preceding `25976` detached ASCII-side record. The first fresh `26196` block-marker bytes only begin at `30808` (`03 00 ad de 28 00 00 00 ...`), and the first fresh post-code payload-side count/header region still begins at block-relative `0x003C`, file offset `30864`, after the 40-byte code window closes.
- The local readable mirror also exposes why that last line still cannot be promoted further. `Lscr-104.blocks.lingo` prints the tail as `// [0x0027] op_05` followed by `_player`, while the raw code bytes end `56 00 00 05`. That mirror-order skew means even the local block mirror does not independently settle whether the slot read should be interpreted before or through opcode `05`, so the deliverable keeps the exact unresolved `_player -- 0x003B` surface instead of guessing a `return _player`.
- The handoff after that `26196` suffix is now pinned more precisely. With the code window ending at block-relative `0x003B`, the very next dword `0x003C..0x003F` is `06 00 00 00`, which closes as the payload-side string-count field for the six recovered name records in `pass-block26196-table.md`. The first actual name-record header begins one step later at `0x0040..0x004B` = `00 00 00 00 04 00 00 00 07 00 00 00` (`id 0`, `kind 4`, `len 7`), and the UTF-16LE `_player` bytes begin at `0x004C..0x0059`, file offset `30880`. So the raw `56 00 00 05` suffix does not border another undecoded executable span; it borders a closed post-code count/header region and then the first proven name record.
- `Lscr-104.json` also exposes several non-late-helper payload-only literal clusters that stay segregated from executable recovery. `CREATECHATROOM` block `3648` carries adjacent literals `rtid`, `rname`, `ipriv`, `memberOnly`, and `isPro`; `RREQ` block `13260` carries `acid`, `baid`, and `bacid`; `RRSP` block `13488` carries `cebt`; and `TRREQUEST` block `22932` carries `challengeeTeamID`, `challengerAccountIDs`, `challengeeAccountIDs`, `challengerAccountCarIDs`, `challengeeAccountCarIDs`, `betAmount`, `isHeadsUp`, and `isRanked`. Their visible code regions and names tables do not slot-bind those strings back into the executable byte spans, so the deliverable treats them as adjacent asset corroboration only and does not rename any `param_*` positions from JSON alone. The raw seam is now tighter too: the trailing `TRREQUEST` tail record for `isRanked` is byte-backed as `01 00 00 00 0F 00 00 00 08 00 00 00 69 73 52 61` at the end of block `22932`, and the next block prefix `6e 6b 65 64` on `TRCOUNTER` `23416` completes that same string before the next `03 00 ad de` block header begins.
- The raw full-block tails tighten that same boundary further. A byte dump of block `13488` still carries trailing ASCII-side payload records `isA` at relative `0x0150` and `cebt` at `0x0160`, but the structured `Lscr-104.json` block object only surfaces `cebt` in its `strings` list and the executable serializer still only proves raw slot pushes `54 00 00` through `54 00 04`. Likewise, a full-tail dump of block `23416` now resolves more precisely than the older note: the tail carries a complete ASCII record `01 00 00 00 01 00 00 00 03 00 00 00 72 69 64 00` for `rid`, followed immediately by a second record `01 00 00 00 03 00 00 00 09 00 00 00 62 65 74 41 6d 6f 75 6e 74 00 00 00` for `betAmount`. The next block `23864` starts with pre-marker bytes `74 00 00 00`, but those are just the final `t` and null padding of that same trailing `betAmount` record before the next `03 00 ad de` header begins. Those contradictions are preserved explicitly: the payload strings are real, but their slot binding back into `RRSP` and `TRCOUNTER` code is still unproven, so no serializer field was renamed from those tails.
- The absence side is also byte-backed now. The structured block objects for `KRT` `10040`, `HTJOIN` `15520`, `HTQRT` `18820`, `HTRT` `20992`, and `CLOSEGAME` `25500` all report `stringCount = 0`, so there is no hidden payload-string cluster left to mine there beyond the proven names table. For those handlers, the remaining blocker is purely opcode/slot semantics, not missed local string recovery.
- `CLOSEGAME` is narrower still than it first looked. Its immediate predecessor `m` `25180` and `CLOSEGAME` `25500` both report `stringCount = 0`, and the raw boundary now shows why: block `25180` ends with a complete ASCII tail-table record `01 00 00 00 15 00 00 00 03 00 00 00 6d 64 35 00`, so `md5` belongs to the trailing payload table of `m`, not to the executable code of `CLOSEGAME`. The next bytes after that null terminator are the fresh `03 00 ad de` header for block `25500`. The body therefore stays a pure slot ladder from `param_ssm` through `param_id_10`.
- The ASCII-side tail tables are now understood a bit more tightly. In blocks such as `1848` (`INQUIRYNIMUSER`), `3648` (`CREATECHATROOM`), `13260` (`RREQ`), and `22932` (`TRREQUEST`), the post-code payload carries a separate block-name-and-entry table after the UTF-16 names table. The visible entry pattern is byte-backed as repeating ASCII records of the form `01 00 00 00 <odd index> <len> <ascii name>`, and the next block's four-byte pre-marker prefix can complete the last trailing string: `2120` contributes `65 00 00 00` to finish `uname`, `4060` contributes `6f 00 00 00` to finish `isPro`, `13488` contributes `62 72 74 00` to finish `brt` from `RREQ`, and `23416` contributes `6e 6b 65 64` to finish `isRanked` from `TRREQUEST`. This proves those tail tables are real block payload, not scanner noise.
- That same tail-table structure is also why the deliverable still refuses to promote many tempting names into executable source. The same record family shows up on handlers whose visible code shape does not match the hidden entry count at all: `INQUIRYNIMUSER` has one visible raw slot push plus one tail entry `uname`, `CREATECHATROOM` has two visible raw slot pushes but seven tail entries (`rtid`, `tid`, `rname`, `ipriv`, `pw`, `memberOnly`, `isPro`), and `RREQ` has no visible argument-slot pushes at all even though its tail table still carries `acid`, `baid`, `bacid`, `bt`, and `brt`. Because the hidden entry tables clearly do not map 1:1 onto the visible serializer opcodes, they are preserved as adjacent payload evidence only and are not promoted into `param_*` renames.
- `HTJOIN` remains a special slot-binding caution. Its top third rounded assignment currently spells the destination as `"after RD"` because the block `15520` names table really does carry string id `17 = after RD`, and the same block later uses that same name id in the explicit `trace("after RD")` call near `0x028F`. But the rounded-write opcode family at the top of the block is still only `54 00 11 ... 55 00 11`, which is byte-for-byte the same slot-write shape used by `KRT`, `HTQRT`, and `HTRT` where the corresponding destination stays generic as `id_17`. The readable body therefore keeps the established reconstruction-backed `after RD` spelling for continuity, while the report still preserves the narrower truth: the trace literal is byte-backed, but the slot-`17` rounded assignment is not independently upgraded beyond the shared raw slot family.
- `Lscr-104.json` does expose adjacent late-helper literals `result`, `flashReturnValue`, `processString`, `processArray`, and `numOfProcesses` in the same evidence cluster that feeds block `27184`, but the raw names table for `__embedded_lscr104_block75` still only proves `trace`, `result: `, `0`, `CheckProcesses`, `_global`, `exeName`, `split`, `,`, `length`, `IsMyProcessNameCorrect`, `flashReturnValue: `, `sprite`, `flashSP`, and `CheckNittoProcessesCB`. Without a byte-backed slot-to-literal binding, those JSON literal values are treated as corroboration only and are not promoted into executable variable renames.
- The new split artifacts `pass80-block26544-split.md`, `pass80-block27184-split.md`, and `pass81-late-helper-cross-block-spill.md` tighten that late-helper corroboration boundary one more step. Block `26544` closes its executable window at file `31214`, then enters payload-side records. Near the end of that same block, payload-side ASCII records for `processString` and `processArray` are fully in-block, and the next record `02 00 00 00 07 00 00 00 0e 00 00 00 6e 75 6d 4f 66 50 72 6f 63 65 73 73` starts `numOfProcesses` at file `31768`. The nominal `27184` pre-marker `65 73 00 00` at file `31792..31795` then contributes the last text bytes `es` plus two null bytes, so the first fresh `27184` marker bytes only begin at `31796` with `03 00 ad de 91 00 00 00 ...`. That means the 4-byte `27184` pre-marker is payload-side spill completion from `26544`, not helper-local executable evidence.
- The embedded late-helper block is narrower than the earlier reconstruction too. `pass-block27184-table.md` plus the raw bytes at file offset `31792 + 16` are now strong enough to keep the nested control itself in readable form: `param_trace = "0"` gates the work region, `CheckProcesses > TRUE` gates the early success assignment, and `58 00 02 57 00 00 51` corrects the old fallback from `trace = "0"` to `trace = 2`. The tail is already as tight as the local bytes prove: the readable artifact keeps `trace(("flashReturnValue: " & trace))` at `0x008F` and `sprite(flashSP).CheckNittoProcessesCB(trace)` at `0x00A4`, and no narrower tail proof emerged beyond that existing callback shape. What remains unresolved there is naming, not control flow.
- The `27184` fallback and callback tail are now byte-partitioned precisely enough to rule out another cleanup pass. The fallback compare/assignment span is `3b 00 09 39 3a 00 00 42 12 07 00 0a 58 00 02 57 00 00 51`, which is exactly the promoted `if IsMyProcessNameCorrect() = 0 then trace = 2` region at `0x0076..0x007D` with no extra standalone write after it. The next two consumers are also direct slot-0 reads rather than residue: `3b 00 00 39 3d 00 0a 56 00 00 1b 3a 00 01 51` drives `trace(("flashReturnValue: " & trace))` at `0x008F`, and `3b 00 0b 39 3d 00 0c 3a 00 01 35 00 0d 39 56 00 00 3a 00 01 51` drives `sprite(flashSP).CheckNittoProcessesCB(trace)` at `0x00A4`. Those tails are already fully consumed by the promoted readable lines, so there is no terminator-only fake line left to drop there either.
- The remaining `27184` uncertainty is therefore no longer in the suffix. After the fallback store at `0x0069..0x006C`, the rest of the block tail is fully accounted for by the promoted `trace(("flashReturnValue: " & trace))` span at `0x006D..0x007B` and `sprite(flashSP).CheckNittoProcessesCB(trace)` span at `0x007C..0x0090`. Any residual ambiguity in `27184` now lives earlier in the compare-family semantics and name binding, not in the callback tail.
- The on-disk closure matches that readable partition exactly. With block file offset `31792`, prefix `4`, and marker `16`, the 145-byte code window starts at file offset `31812` and ends at `31956`. The fallback store `0x0069..0x006C` sits at `31917..31920`, the `flashReturnValue` trace consumer `0x006D..0x007B` sits at `31921..31935`, and the final sprite callback tail `0x007C..0x0090` occupies `31936..31956`, which is the end of the code window. There are no hidden bytes after the callback tail, so no additional suffix cleanup line is available to promote or delete there.
- Unlike `26196`, the local mirror does not introduce a second tail-order ambiguity for `27184`. The final visible `blocks.lingo` lines are the same two tail consumers already promoted in the readable artifact, and the raw bytes end on the callback write `... 56 00 00 3a 00 01 51` with no local `op_05`-style terminator marker after them. So the `27184` suffix is closed as-is: no extra readable tail line remains to recover there.
- `IsMyProcessNameCorrect` still carries a real contradiction that the deliverable does not smooth over. The 11-byte code region only proves the `_player` / `quit` call shape, but both `Lscr-104.json` and the raw block tail attach a trailing `returnValue` string record to block `25976`. There is still no byte-backed slot binding from that trailing `returnValue` record back into the executable 11-byte region, so the promoted body remains `_player.quit()` even though block `27184` uses `IsMyProcessNameCorrect()` in a boolean-ish compare.
- That `25976` contradiction is now closed at the file-byte layer more tightly too. With block file offset `30584`, prefix `4`, and marker `16`, the 11-byte code window starts at file offset `30604` and ends at `30614`, exactly matching `3b 00 00 35 00 01 39 3a 00 00 51` for `_player.quit()`. The final detached ASCII-side segment begins much later at block-relative `0x00C8`, file offsets `30784..30803`, as `02 00 00 00 01 00 00 00 0b 00 00 00 72 65 74 75 72 6e 56 61`. That span is still non-executable, and it only carries the leading fragment `returnVa`. The next block's pre-marker bytes for `26196`, `6c 75 65 00` at file offsets `30804..30807`, complete the spilled ASCII `returnValue` plus null terminator before the fresh `26196` block marker `03 00 ad de 28 00 00 00 ...` begins at `30808`. So `returnValue` is now pinned as a cross-block detached tail record from `25976`, not as an omitted readable line inside its executable window.
- The post-code gap inside `25976` is closed more tightly now too. After the last executable byte at block-relative `0x001E`, there is exactly one alignment byte `0x001F = 00`, then the payload-side string-count dword `0x0020..0x0023 = 02 00 00 00`, matching the two recovered name records in `pass-block25976-table.md`. The first actual name-record header begins at `0x0024..0x002F` = `00 00 00 00 04 00 00 00 07 00 00 00` (`id 0`, `kind 4`, `len 7`), and the UTF-16LE `_player` bytes begin at `0x0030..0x003D`, file offset `30632`. The next recovered name record `quit` begins with its header at `0x0040` and its UTF-16LE bytes at `0x004C`, file offset `30660`. So there is no undecoded executable tail between `_player.quit()` and the later detached `returnValue` segment at `0x00C8`; the contradiction is strictly between the closed 11-byte executable window and a much later cross-block payload record.
- Block `75` remains embedded and unnamed beyond `__embedded_lscr104_block75`; no additional handler name was invented.

Verification:
- Inventory, raw-evidence, reconstruction, readable-Lingo lift, and JS mirror verification were not blocked.
- JS helper omission from `final-clean` is intentional.
