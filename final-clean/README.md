# final-clean

This tree keeps only the two requested deliverables per script folder:
- `<script>.mechanical-report.md`
- `rightful.ls`

The reports in this tree were regenerated from current local evidence instead of being treated as blind copies. Existing staged reports were used only as corroboration where they already matched the local JSON and `rightful.ls` surfaces.

| Script | `rightful.ls` source tree | Report status in `final-clean` | JS mirror status | Deliverable status |
|---|---|---|---|---|
| `Lscr-93` | `reconstructed (1)\Lscr-93\rightful.ls` | rewritten from local evidence | primary mirror at `clean\Lscr-93\root-artifacts\Lscr-93.rebuilt-faithful.js` | recovered-body deliverable |
| `Lscr-101` | `reconstructed\Lscr-101\rightful.ls` | rewritten from local evidence | no local same-block JS mirror found | recovered-body deliverable |
| `Lscr-102` | `reconstructed (1)\Lscr-102\rightful.ls`, plus final-clean lifts from `Lscr-102.bin-only.pass1-report.md`, `Lscr-102.annotated-disasm.md`, `Lscr-102.blocks.lingo`, and `Lscr-102.blocks.js` | newly written from local evidence | primary mirror at `clean\Lscr-102\rightful.full-mechanical.js` | recovered-body deliverable |
| `Lscr-103` | `reconstructed (1)\Lscr-103\rightful.ls`, plus final-clean lifts from `Lscr-103.blocks.lingo` and `pass-block*.md` | rewritten from local evidence | primary mirror at `clean\Lscr-103\root-artifacts\Lscr-103.rebuilt-faithful.js` | recovered-body deliverable |
| `Lscr-104` | `reconstructed (1)\Lscr-104\rightful.ls`, plus final-clean lifts from `pass-block*.md` tables | rewritten from local evidence | primary mirror at `clean\Lscr-104\root-artifacts\Lscr-104.rebuilt-faithful.js` | recovered-body deliverable |
| `Lscr-105` | `reconstructed (1)\Lscr-105\rightful.ls`, plus final-clean `EMPTY` lift from `Lscr-105.stack-faithful.lingo` | rewritten from local evidence | primary mirror at `clean\Lscr-105\root-artifacts\Lscr-105.rebuilt-faithful.js` | recovered-body deliverable |
| `Lscr-106` | `reconstructed (1)\Lscr-106\rightful.ls`, plus final-clean constructor lifts from `Lscr-106.blocks.lingo` | rewritten from local evidence | primary mirror at `clean\Lscr-106\root-artifacts\Lscr-106.rebuilt-faithful.js` | recovered-body deliverable |
| `Lscr-107` | `reconstructed (1)\Lscr-107\rightful.ls`, plus final-clean lifts from `Lscr-107.blocks.lingo` and `Lscr-107.pass-chain-templates.md` | rewritten from local evidence | primary mirror at `clean\Lscr-107\root-artifacts\Lscr-107.rebuilt-faithful.js` | recovered-body deliverable |
| `Lscr-108` | `root-artifacts\Lscr-108.decompiled.lingo`, promoted over the appendix-only `reconstructed (1)\Lscr-108\rightful.ls` | newly written from local evidence | accepted same-body mirror at `clean\Lscr-108\root-artifacts\Lscr-108.decompiled.js` | recovered-body deliverable |
| `Lscr-109` | staged `clean\Lscr-109\rightful.ls` readable body; not hash-equal to either evidence tree copy | rewritten from local evidence | primary mirror at `clean\Lscr-109\root-artifacts\Lscr-109.rebuilt-faithful.js` | recovered-body deliverable |

Notes:
- The first copy stage produced exact `clean`-body matches in `final-clean`; later byte-backed promotion passes intentionally changed only the scripts called out below.
- Current final-clean body divergences from the staged `clean` body are now:
- `Lscr-93`: final `showConnectionErrorCB("Connection Error", ...)` tail now carries the byte-backed `netError[_global.gNetID[_global]]` term at `0x2C5D..0x2C6D`, the earlier literal-slot cleanups, nine standalone `_global` residue removals at `0x0535`, `0x0549`, `0x0564`, `0x057F`, `0x059A`, `0x06BE`, `0x06D2`, `0x06E6`, and `0x0711`, and a second early-login cleanup set at `0x0087`, `0x0104`, `0x012E`, and `0x0183`; the remaining windows `0x00EE..0x0101`, `0x0105..0x011C`, `0x012F..0x0168`, and `0x016E..0x0180` stay blocked as packed control/prefix machinery.
  - `Lscr-102`: promotes `brakeDecleration = -40` and `revLimiterDeceleration = -30` from `Lscr-102.bin-only.pass1-report.md`, and `xtra.parseString(param_xtra)` plus `xmlparser = xtra.makeList().n2` in block `5972`.
- `Lscr-103`: promotes `_global.objRace.rpm = (initBoost * 100)`, preserves unresolved local `id_5` instead of staged fake target `-5`, keeps `_global.push(id_5)` byte-backed, treats the `id_5` store tail as a continuous raw-backed region from `0x0140..0x0151` with no intervening executable line between the local-slot-`5` store and `_global.push(id_5)`, and now explicitly treats the weaker `blocks.lingo` `op_0f` surface in block `2788` as a lower-fidelity rendering of the proven `if ... then boostSetting.push(FALSE) else boostSetting.push(TRUE)` jump pair rather than a third standalone statement.
  - `Lscr-104`: promotes the explicit compare `if not (_player.debugPlaybackEnabled = TRUE) then` in `CheckNittoProcessesCB` and `trace = FALSE` in embedded block `27184`.
  - `Lscr-105`: promotes the `HQOK` branch middle argument at `0x15AD` from `id_154` to byte-backed `EMPTY`.
  - `Lscr-106`: promotes constructor sites from placeholder forms to direct `Array()` / `Object()` forms where the left-hand targets are byte-backed.
- `Lscr-107`: promotes constructor sites from placeholder forms to direct `_global... = Array()` forms where the left-hand targets are byte-backed, carries the first controlled lift from `Lscr-107.pass-chain-templates.md` into direct receiver-carried lines for `_global.aid = VOID`, `_global.mainURL = "http://game.nittolegends.com/"`, `_global.gameURL = (_global.mainURL & "gameCode1_00.aspx")`, `_global.avatarMainURL = _global.mainURL`, `_global.cacheURL = "http://download.nittolegends.com/"`, `_global.socketURL = "live.nittolegends.com"`, `_global.uggURL = "http://ugg.nittolegends.com/"`, `_global.exeName = NittoLegendsBeta`, `_global.isEnc = 1`, `_global.isIdle = 0`, and `_global.isConnected = 0`, lifts `sprite(flashSP).rect = rect` as the single proved `setInactive` tail at `0x0089`, removes the fake pre-callback `_global` line before `sprite(flashSP).noActivityCB()` in `sendHeartbeat`, removes the fake trailing `_global` after `isConnected = 0`, removes the fake trailing `_global` from `resizeWindow`, and removes the final fake standalone `_global` from embedded block `4716`.
- `Lscr-107` now also keeps only the genuinely unresolved store payloads explicit: in `startMovie` block `152`, `_global.lastAction = id_11`, `_global.socketBuffer = id_11`, `_global.facebookToken = id_11`, `_global.facebookID = id_11`, `_global.facebookSessionSecret = id_11`, and `_global.facebookSessionKey = id_11`; in embedded block `4716`, `_global.facebookToken = id_2`, `_global.facebookID = id_2`, `_global.facebookSessionSecret = id_2`, and `_global.facebookSessionKey = id_2`. After the controlled lift, the only separator-style residue still left in `startMovie` is the trailing `_global -- 0x0112`, which remains unpromoted because `Lscr-107.pass-chain-templates.md` does not prove whether it is a live receiver prefix or a terminator-only residue.
- `Lscr-101` did not receive a new `rightful.ls` lift in the latest sweeps, but its report is now tighter about raw handler offsets, source-line anchors, `handlerVectorsCount = 0`, the lack of a local same-body JS artifact, and the literal-side overlap blocker around `1528..1602`, including the exact overlap bytes `1590..1591` between the tail-entry decode and the first confirmed string record.
- `Lscr-102` also received a report-only follow-up after the code lifts above: block `43264` / `RaceEngine_buildMd5Source` now calls out the anti-lag contradiction at `0x0619` and `0x063E` directly in both deliverables, preserving the conflict between the raw `1982773` rows and the faithful/full `0.7745` view instead of smoothing it into a looser note.
- `Lscr-105` and `Lscr-106` also received report-only tightening in the latest sweep: `Lscr-105` now pins the unresolved object-tail offsets and confirmed `TRA` resets exactly, and `Lscr-106` now ties each surviving `id_*` / `implicit_*` blocker to specific block-table gaps and preserves the `fetchCacheXMLDone` contradiction between the two reconstruction trees.
- `Lscr-103` also received a tail-control report tightening in block `2788`: the raw slice `64 00 04 51 06 FE D5` is now pinned to `0x01BF..0x01C5`, with local slot `4` proven as `initBoost` while the exact repeat-step opcode semantics stay unresolved.
- `Lscr-103` now also pins the callback-start seam immediately after that unresolved tail: the backward-jump slice ends at raw `0x01C5`, and the very next byte at raw `0x01C6` already begins the resolved `sprite(flashSP)` receiver-setup span `3b 00 1e 39 3d 00 1f 3a 00 01` before the later `garageDynoRunCB` close. No spacer opcode or third executable line sits between those regions.
- `Lscr-103` now also pins the callback body itself end to end: the resolved receiver setup closes at raw `0x01CF`, the first callback-argument load begins immediately at raw `0x01D0`, the final 4-argument call close ends at raw `0x0208..0x020B`, and trailer bytes begin immediately at raw `0x020C`, so no additional executable line exists before, within, or after the callback body.
- `Lscr-107` also received tighter blocker mapping after those tail cleanups:
  - block `152`: missing `id_11` at `0x0062`, `0x00BC`, `0x00F1`, `0x00FB`, `0x0105`, `0x010F`
  - block `152`: the staged `_global` residue candidates at `0x0037`, `0x003F`, `0x0065`, `0x006F`, `0x0080`, `0x008D`, `0x0097`, `0x00A1`, `0x00AB`, and `0x00B5` stay in place because the raw bytes still tie them to live receiver-load chains rather than terminator-only tails
  - block `152`: the tail after `avatarMainURL = _global.mainURL` remains live because the raw continuation `... 36 00 10 51 3b 00 02 3d 00 12 36 00 11 51 ...` proves the staged `_global` at `0x0080` / `0x008D` still belongs to the following `_global.cacheURL = "http://download.nittolegends.com/"` store chain
  - block `4716`: missing `id_2` at `0x001A`, `0x0024`, `0x002E`, `0x0038`
  - block `4716`: the final staged standalone `_global -- 0x003B` is now gone because the raw tail ends `... 3b 00 00 3d 00 02 36 00 05 51`, so `facebookSessionKey = id_2` already closes the last live chain
  - block `3060`: packed callee region still unresolved at `0x0072..0x0078`
  - block `4260` / `openURL`: the raw body `3b 00 00 35 00 01 20 07 00 0e 3b 00 02 39 3d 00 03` is too short to prove any additional terminator-only tail cleanup beyond the current guarded `ssm(H)` surface
  - block `1912` / `stopMovie`: the raw body `3b 00 00 39 3a 00 00 51 3b 00 01 3b 00 03 39 23 00 00` closes inside the existing `resizeFlashSprite()` plus `_global.objRace = RaceEngine` chain, so no extra standalone tail line can be removed there
  - block `4496` / `clearFB`: the raw body `3b 00 00 39 3b 00 01 39 54 00 00 3a 00 01 3d 00 02` ends before any final `51`-terminated tail appears, so the current single `gotoNetPage(unescape(param_gotoNetPage), _blank)` lift stays the narrowest honest body
  - blocks `2680` / `zoomWindow` and `2860` / `resizeFlashSprite`: both raw bodies are exactly `3b 00 00 39 3a 00 00 51`, so each handler is already just the single `resizeFlashSprite()` call chain and has no extra standalone tail to remove
  - short-tail exhaustion status: cleaned where proof existed (`resizeWindow`, `sendHeartbeat`, embedded block `4716`, and the `setInactive` property-write tail), exhausted but unchanged where no fake final standalone line exists (`zoomWindow`, `resizeFlashSprite`, `stopMovie`, `openURL`, `clearFB`), and still off-limits under this cleanup rule (`startMovie`, plus the non-final `_global` separators earlier in embedded block `4716`)
- `Lscr-108` is no longer blocker-limited. The appendix-only staged body was replaced by the stronger local bytecode-backed decompiled surface, promoting full readable handler bodies for `lingoXtraNoDebug`, `lingoXtraInit`, `getPCInfo`, the wrapper `baLingo*` handlers, and `isCheatEngineRunning`, with accepted same-body JS corroboration from `Lscr-108.decompiled.js`. The report now also pins the return convention exactly: `lingoXtraNoDebug` and `lingoXtraInit` end in plain `ret`, while `getPCInfo`, `isCheatEngineRunning`, and all eleven wrappers terminate through the same `pusharglistnoret 1`, `extcall 156`, `ret` veneer.
- `Lscr-108` also now carries byte-range partition maps for each substantive handler, separating resolved islands from opaque clusters end to end: `lingoXtraNoDebug` (`0..0`, `13..45`, `64..67` resolved), `lingoXtraInit` (`0..0`, `12..17`, `27..29`, `39..39` resolved), `getPCInfo` (`0..2`, `26..28`, `52..61`, `101..117`, `142..142` resolved), and `isCheatEngineRunning` (`0..14`, `24..31`, `43..43` resolved).
- `Lscr-108` now also separates true repeated raw-id families from singleton opaque ids: repeated families include `setmovieprop 138`, `extcall 140`, `extcall 148`, `extcall 149`, `pushsymb 150`, `objcall 151`, `extcall 152`, and especially `extcall 156`; singleton opaque ids are now split by handler instead of being left as one undifferentiated unresolved pool.
- `Lscr-108` now also separates handler-local repeated-id families from cross-handler ones: `extcall 149`, `pushsymb 150`, `objcall 151`, `extcall 152`, and the return-veneer `extcall 156` are now pinned as handler-local families, while `setmovieprop 138`, `extcall 140`, and `extcall 148` are the only repeated ids currently proven to cross handler boundaries.
- `Lscr-108` now also pins those family-locality splits more tightly: `extcall 149`, `pushsymb 150`, `objcall 151`, and `extcall 152` stay entirely inside `getPCInfo`; `extcall 156` stays in return-veneer territory only (`getPCInfo`, both `isCheatEngineRunning` arms, and all wrappers); and only `setmovieprop 138`, `extcall 140`, and `extcall 148` currently cross handler boundaries at all.
- `Lscr-109` did not gain a new executable lift in the latest local pass, but its report now pins the remaining `executeCall` and embedded-block trap families to exact byte windows and raw table ids instead of leaving those blockers generic.
- `Lscr-109` is the exception: the promoted readable body comes from the staged `clean` surface and does not hash-match either `evidence\reconstructed\rightful.ls` or `evidence\reconstructed (1)\rightful.ls`.
- The reports point back to the byte-pattern layer explicitly: where decoded block tables exist, offset `N` maps to `clean\Lscr-XXX\root-artifacts\Lscr-XXX.pass-blockN-table.md`.
- `Lscr-93` now fences the two late loop tails more precisely: first tail marker spans `0x2CB4..0x2CB7` and `0x2CB8..0x2CBB`, then back-jump `0x2CBC..0x2CBE`, with the first explicit post-tail write only beginning at `0x2CBF..0x2CC3`; second tail marker spans `0x2E58..0x2E5B` and `0x2E5C..0x2E5F`, then back-jump `0x2E60..0x2E62`, with the next proven executable boundary only resuming at `0x2E63..0x2E7D`.
- `Lscr-93` also now fences the late Facebook invite/callback strip line-by-line: `OK = checkedForOneClient.getAProp("d")` at `0x2814..0x2824`, then `trace("here!")`, `trace("info list!")`, `trace(OK)`, and `openURL(..., _blank)` through `0x2853`, with the following jump only beginning at `0x2854..0x2856`. Each of those calls consumes its own terminating `op_51`, so none is removable under the terminator-only rule.
- `Lscr-93` now also pins the late first-tail `case` table seam more tightly: `0x2D5F..0x2D73` is still a packed raw arm table with visible anchors at `0x2D60`, `0x2D64`, `0x2D68`, `0x2D6C`, and `0x2D70`, and the first readable target body still does not begin until `0x2D74`.
- `Lscr-93` now also closes the sub-boundary question inside that packed seam: `block1.annotated-disasm.md` stays opaque at `0x2D71`, `0x2D72`, and `0x2D73`, while the first readable body still begins only at `0x2D74` (`3B 00 04 ; "netError"`), and both `pass15-action-span-inventory.md` and `pass6-whole-file-region-map.md` still corroborate the same `before 0x2D74` split. So `0x2D73` remains best treated as table payload, not a hidden body start.
- `Lscr-93` now also resolves the role of those final packed bytes more tightly even though the seam itself does not move: `0x2D70..0x2D71` is the last record's 16-bit id word (`0x01A9`, table id `425`, `installer`), and `0x2D72..0x2D73` is that same record's 16-bit relative target word (`0x00D4`), which lands at `0x2E2F`, the already-readable `installer` body start surfaced in `rightful.ls`. So `0x2D73` is specifically the low byte of the final packed target word, not an unclassified free byte.
- `Lscr-104` now closes the remaining direct-slot search more tightly in report form: no instruction-region `56 00` or `57 00` spans remain in `JOINRACECHAT`, `LEAVERACECHAT`, or `TEAMKICK` beyond the same-slot lifts already promoted, and the only surviving tail-specific late-helper blocker is `CheckNittoProcesses` block `26196`, where the final raw `... 43 57 00 00 51 56 00 00 05` still does not independently prove an explicit `return _player`.
- `Lscr-104` now also pins the `26196` suffix and `27184` tail spans more exactly: `26196` is closed as one fallback store `0x001F..0x0023`, one terminal slot read `0x0024..0x0026`, and one lone `op_05` terminator marker `0x0027`; `27184` is closed as a fallback compare/assignment region `0x005A..0x006C`, promoted readable compare/assignment target `0x0076..0x007D`, and direct slot-0 tail consumers at `0x008F` and `0x00A4`.
- `Lscr-104` now also pins those late-helper spans back to their on-disk code windows: `CheckNittoProcesses` block `26196` occupies file bytes `30824..30863`, with the unresolved `56 00 00 05` suffix narrowed to the final code bytes `30860..30863`; embedded block `27184` occupies file bytes `31812..31956`, with the final callback consumer closed at `31936..31956`, so no post-callback suffix survives there.
- `Lscr-104` now also closes the detached `returnValue` contradiction in file-byte terms: `IsMyProcessNameCorrect` executes only across `30604..30614` (`3b 00 00 35 00 01 39 3a 00 00 51`), while the later `returnValue` bytes sit separately at block-relative `0x00D0`, file offsets `30792..30807`, immediately before the next block header for `26196`, so that record stays non-executable tail data rather than a missed readable line.
- `Lscr-104` now also pins the immediate post-code handoff bytes on both late helpers: in `CheckNittoProcesses`, executable bytes still end at block-relative `0x0027` / file `30863`, post-code bytes `0x0028..0x0037` are `06 00 00 00 00 00 00 00 04 00 00 00 07 00 00 00`, and block-relative `0x0038..0x0045` already begins the UTF-16LE `_player` payload/name-record region; in `IsMyProcessNameCorrect`, executable bytes still end at block-relative `0x000A` / file `30614`, post-code bytes `0x000B..0x001B` are `00 02 00 00 00 00 00 00 00 04 00 00 00 07 00 00 00`, and block-relative `0x001C..0x0029` already begins the UTF-16LE `_player` record before the later `quit` and detached `returnValue` payload entries.
- `Lscr-104` now also tightens those handoffs to the exact payload starts and spill boundary: in `CheckNittoProcesses`, the raw suffix `56 00 00 05` still ends the executable window at file `30860..30863`, the next payload-side count dword begins immediately at `30864..30867`, the first UTF-16LE name-record header begins at `30868..30879`, and the first UTF-16LE `_player` bytes start at `30880`; in `IsMyProcessNameCorrect`, there is exactly one alignment byte after code, the payload-side string-count dword begins at `30616..30619`, the first UTF-16LE `_player` bytes begin at `30632`, `quit` begins at `30660`, and the detached `returnValue` tail is now pinned as a cross-block spill whose in-block fragment begins at `30784` and whose final `6c 75 65 00` bytes spill into the next block premarker at `30804..30807`.
- `Lscr-104` now also proves that spill boundary is maximal: the `25976` detached tail fragment at `30784..30803` carries only header bytes plus leading ASCII `returnVa`, the `26196` premarker bytes `6c 75 65 00` at `30804..30807` complete the spill as `lue\0`, and the first fresh `26196` block marker begins immediately at `30808`, so the boundary cannot move earlier or later without either truncating `returnValue` or consuming the next block marker.
- `Lscr-102` now separates strict block-surface stops from broader annotated continuations more explicitly: block `37252` keeps faithful/full-only residue at `0x0145` / `0x0173` separate from the real packed handoff core `0x016F..0x0174`, while block `43264` keeps the broader gate anchored at `0x05DD`, the minimal gate core at `0x05F4..0x05F5`, the strict block-surface tail stop at `0x064F`, and the broader annotated continuation through `0x065A` and `0x0663`.
- `Lscr-102` now also tightens the `KDONE` contradiction in block `14012` without inventing a lhs: `pass-block14012-table.md` proves payload id `145 = KDONE`, but that proof lands later in the block and still does not expose a named lhs inside the raw `0x1135` compare window; at `0x11C5`, `annotated-disasm.md` carries `op_3B arg=145`, while the stricter `block08-islands.js` surface still leaves the lhs as `/*stack*/`, so the deliverable preserves that contradiction explicitly.
- `Lscr-107` now also pins the missing-value store windows directly instead of only listing the unresolved ids: in `startMovie` block `152`, the six unresolved `id_11` writes are `_global.lastAction` `0x0048..0x0053`, `_global.socketBuffer` `0x00A4..0x00AF`, `_global.facebookToken` `0x00DC..0x00E7`, `_global.facebookID` `0x00E8..0x00F3`, `_global.facebookSessionSecret` `0x00F4..0x00FF`, and `_global.facebookSessionKey` `0x0100..0x010B`; in embedded block `4716`, the four unresolved `id_2` writes are `_global.facebookToken` `0x0000..0x000D`, `_global.facebookID` `0x000E..0x001B`, `_global.facebookSessionSecret` `0x0014..0x0021`, and `_global.facebookSessionKey` `0x001E..0x002B`.
- `Lscr-108` now also splits the repeated raw-id families by operational role instead of only by locality: `pushsymb 150` is the cluster-entry family inside `getPCInfo`, `objcall 151` is the matching cluster-exit family, `extcall 149` is the cross-cluster bridge inside `getPCInfo`, `extcall 152` is the interior-repeat family inside the CPU/mac cluster, and `extcall 156` remains confined to the return-veneer family only.
- `Lscr-108` now also pins exact control-flow island edges for the three JSON branch sites: in `lingoXtraNoDebug`, predicate `49..57`, taken island `60..62`, and proven standalone tail start at `64`; in `getPCInfo`, predicate `18..28`, taken island `31..52`, and fallthrough query-build restart at `54`; in `isCheatEngineRunning`, predicate `20..26`, true arm `29..33`, join skip `35`, false arm `38..41`, and plain `ret` at `43`, closing off any hidden false-arm tail.
- `Lscr-108` now also tightens the branch joins themselves: in `getPCInfo`, the taken island closes with its join writeback `setlocal 0` at `52`, and `jmpifz 26@28` skips directly to the one-sided post-branch resume at `54`; in `isCheatEngineRunning`, the true arm exits through `extcall 156@33` and `jmp 8@35`, while the false arm falls through from `jmpifz 12@26` into `38..41` before the shared `ret@43`.
- `Lscr-108` now also closes the last comparable join seam in `lingoXtraNoDebug`: `jmpifz 7@57` skips directly to the shared post-branch tail at `64`, while the taken island falls straight through from `extcall 142@62` into that same tail, so there is no mirrored false-arm body hidden before `myFile = 0`.
- `Lscr-108` now reaches full join-style classification across all substantive handlers: `lingoXtraNoDebug` and `getPCInfo` are one-sided branches with no mirrored false arm before their shared resumes, `isCheatEngineRunning` is the only two-arm branch with an explicit `jmp 8` join skip into a shared `ret`, and `lingoXtraInit` has no branch site to classify.
- The reports in this tree follow the same recovery order for each script: inventory pass, raw-evidence pass, reconstruction pass, readable-Lingo lift pass, and JS mirror pass.
- Helper JS files were intentionally not surfaced in `final-clean`, even when local mirrors exist.
